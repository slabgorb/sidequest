# ADR-vs-Code Audit — 2026-06-03

> Generated by the `verify-adr-claims` multi-agent workflow (run `wf_0f9ed063-1a3`). 113 non-superseded ADRs (status `accepted`/`proposed`) each read in full, verified against current code across all four subrepos, and every disagreement put through an adversarial refutation pass. **65 confirmed findings, 48 agree, 0 can't-verify.** 186 agents, ~8.95M tokens. Findings below survived adversarial refutation.

**Severity:** 18 major · 40 minor · 7 trivial  
**Category:** 11 overstated · 24 unrecorded-impl · 30 stale-info · 0 split-brain

## Overstated — claimed `live`, code missing/partial/unwired (11)

### ADR-021 · MAJOR · Progression System
- **Claimed:** `accepted/live`
- **Finding:** The ADR's four progression tracks are partially implemented at the data-model layer (ProgressionConfig, AffinityState, ItemEvolution, WealthTier all exist and load from YAML) but tracks 1-3 have no live engine wiring: milestone accumulation/level-up is a TODO, AffinityState tier promotion is explicitly P6-deferred in code comments, and item narrative_weight thresholds are explicitly P2-deferred with zero runtime consumers. Only track 4 (journey recap via _generate_recap/PgSnapshot.load) is fully live. Claiming the whole system "live" overstates reality.
- **Evidence:** sidequest-server/sidequest/game/character.py:55-57,102-103 (AffinityState P6-deferred); sidequest-server/sidequest/genre/models/progression.py:62 ("TODO: wire at level-up"); sidequest-server/sidequest/game/item_catalog_resolution.py:31 ("no runtime consumer reads item narrative_weight yet P2-deferred"); sidequest-server/sidequest/genre/models/progression.py:187-221 (WealthTier and milestone_categories exist only as data models — no production consumer maps gold to wealth label or accumulates milestones to level-up); sidequest-server/sidequest/game/persistence.py:141-178 and sidequest-server/sidequest/game/pg/snapshot.py:188 (Journey/recap is live)

### ADR-024 · MAJOR · Dual-Track Tension Model
- **Claimed:** `accepted/live`
- **Finding:** The TensionTracker class is fully implemented in tension_tracker.py with both tracks, event spikes, pacing_hint(), and OTEL emission, but it is never instantiated in any production code path. The orchestrator declares a pacing_hint: PacingHint | None = None field and has the consumption logic (register_pacing_section call at orchestrator.py:2650), but nothing in the server ever sets this field — so the narrator always runs with pacing_hint=None and the dual-track signal never reaches any consumer. The ADR claims status=live, but the wiring between TensionTracker and the narrator prompt pipeline is absent.
- **Evidence:** sidequest-server/sidequest/game/tension_tracker.py:266 — TensionTracker class exists with full implementation; sidequest-server/sidequest/agents/orchestrator.py:728 — pacing_hint field declared as PacingHint | None = None; orchestrator.py:2648-2653 — register_pacing_section only called when context.pacing_hint is not None; no production code ever instantiates TensionTracker() or calls .pacing_hint() — confirmed by grep for TensionTracker( and .pacing_hint= across all sidequest/ production files; encounter.py:65 has an unrelated drama_weight() on EncounterPhase that is also never called externally

### ADR-025 · MAJOR · Pacing Detection
- **Claimed:** `accepted/live`
- **Finding:** The infrastructure for pacing detection is present (TensionTracker, PacingHint, register_pacing_section, TurnContext.pacing_hint field, injection guard in build_narrator_prompt) but is completely unwired: TensionTracker is never instantiated in production code, pacing_hint is never set on TurnContext (defaulting to None on every turn), so the [PACING] hint is never injected into any narrator prompt. Additionally, the ADR's original quiet-turn detection (comparing pre/post game state on location/combat/NPCs/quest) has no implementation whatsoever, and the player-action accelerator/decelerator scanning is explicitly deferred in trope_tick.py with a "future sub-story" comment.
- **Evidence:** sidequest-server/sidequest/game/tension_tracker.py:266 (TensionTracker defined, never instantiated in production); sidequest-server/sidequest/server/session_helpers.py:1158 (the only TurnContext() construction site — pacing_hint not passed, stays None every turn); sidequest-server/sidequest/agents/orchestrator.py:728,2648 (field declared, injection guarded by `if context.pacing_hint is not None` — never fires); sidequest-server/sidequest/game/trope_tick.py:226-230 (accelerator/decelerator keyword matching explicitly commented as "left to a future sub-story")

### ADR-40 · MAJOR · Narrative Character Sheet (No Raw Stats)
- **Claimed:** `accepted/live`
- **Finding:** The ADR claims a NarrativeSheet type (implemented in sidequest-game/src/narrative_sheet.rs, translated to Python post-port) enforces at the type level that no raw numerical values leave the game layer, with describe_health() banding HP into six narrative states. In reality, no narrative_sheet.py or NarrativeSheet class exists; the actual protocol type is CharacterSheetDetails with stats: dict[str, int] (raw ability scores) that flows unchanged to the UI, which renders raw numeric values on screen. The HP amendment in ADR-114 accounts for raw HP being deliberately exposed, but the core no-raw-stats invariant for ability scores was never implemented in Python at all.
- **Evidence:** sidequest-server/sidequest/protocol/models.py:392-422 (CharacterSheetDetails carries stats: dict[str, int] — raw integers); sidequest-server/sidequest/server/views.py:355,384 (raw stats dict passed directly into CharacterSheetDetails); sidequest-server/sidequest/protocol/models.py:445-447 (PartyMember.current_hp: int, max_hp: int); sidequest-server/sidequest/server/views.py:485-486 (current_hp=character.core.hp.current, max_hp=character.core.hp.max); sidequest-ui/src/components/CharacterSheet.tsx:153-158 (stats rendered as raw numbers), CharacterSheet.tsx:185 ({data.hp}/{data.hp_max}); no narrative_sheet.py or NarrativeSheet class exists anywhere in sidequest-server/; no describe_health() function exists anywhere in production code

### ADR-049 · MAJOR · Narrator Verbosity and Vocabulary (Two-Axis Text Tuning)
- **Claimed:** `accepted/live`
- **Finding:** The prompt-injection plumbing (_build_verbosity_section/_build_vocabulary_section called at every narrator turn) is live, but the player-control layer is absent: VerbositySlider.tsx and VocabularySlider.tsx do not exist in the UI, TurnContext in session_helpers.py hardcodes the defaults instead of reading from the CONNECT payload, and default_for_player_count is defined but never called in production code. Additionally, the ADR claims both enums implement default_for_player_count, but NarratorVocabulary only has a plain default() method.
- **Evidence:** sidequest-server/sidequest/protocol/enums.py:133,154,165,181 — enums and default_for_player_count defined for NarratorVerbosity only (NarratorVocabulary has no such method); sidequest-server/sidequest/agents/orchestrator.py:1423,1471,2328-2329,2636-2637 — _build_verbosity_section/_build_vocabulary_section exist and fire in prompt assembly; sidequest-server/sidequest/server/session_helpers.py:1167-1168 — TurnContext is hardcoded to narrator_verbosity="standard"/narrator_vocabulary="literary" regardless of CONNECT payload; sidequest-server/sidequest/handlers/connect.py:1402-1403 — narrator_verbosity=None/narrator_vocabulary=None on slug-resume; sidequest-ui/src/types/protocol.ts:128,131 — type aliases exist but no component imports them; no VerbositySlider.tsx or VocabularySlider.tsx found anywhere in sidequest-ui/src/

### ADR-052 · MAJOR · Narrative Axis System (/tone Command)
- **Claimed:** `accepted/live`
- **Finding:** The ADR claims the system is live: genre packs declare narrative_axes, players shift tone via /tone set, a ToneCommand is wired into SlashRouter, and active axis modifiers are injected at narrator prompt construction time. In reality, the AxesConfig model and axes.yaml files exist and are loaded, but pack.axes is never accessed at runtime — no ToneCommand exists in BUILTIN_COMMANDS, /tone is silently swallowed client-side as an unknown slash command, axis_values on GameSnapshot is explicitly annotated "P2-deferred" in source, and there is no axis-modifier injection anywhere in the narrator prompt pipeline. The ADR's implementation-status=live is incorrect; this is a deferred subsystem with skeletal data models only.
- **Evidence:** sidequest-server/sidequest/game/session.py:540-544 (AxisValue is "P2-deferred"), session.py:794-795 (axis_values field comment "P2-deferred: narrative axis values"), sidequest-server/sidequest/game/commands.py:403-410 (BUILTIN_COMMANDS has no ToneCommand), sidequest-ui/src/hooks/useSlashCommands.ts:49-53 (/tone falls into default "swallow client-side, no server round-trip"), sidequest-server/sidequest/genre/loader.py:1266+1491 (axes.yaml loaded into GenrePack.axes), but zero production code reads pack.axes outside imports/models (confirmed by grep of .axes usage)

### ADR-121 · MAJOR · Layered Content Resolution — Global→Genre→World→Culture Merge with Per-Field Strategies and Provenance
- **Claimed:** `accepted/live`
- **Finding:** The ADR claims `implementation-status: live` for the full Global→Genre→World→Culture four-tier merge walk via `Resolver[T].resolve_merged`. The class and method exist at the exact line numbers cited (resolver.py:255-390), but `Resolver` is never instantiated and `resolve_merged` is never called from any production code path — it is exported from `genre/__init__.py` but has no live consumers. The only production resolution path uses `shim.py::resolve_archetype`, which performs a two-tier lookup (world funnel → genre fallback) bypassing the Resolver machinery entirely. The supporting types — `LayeredMerge`, `MergeStrategy`, `Provenance`/`MergeStep` — are live and correctly described, and `archetype_provenance` does ride the wire via `chargen_mixin.py`; the overstated claim is specifically the four-tier `Resolver.resolve_merged` walk being called "live" when it is dead code with no production callers.
- **Evidence:** sidequest-server/sidequest/genre/resolver.py:200-390 (Resolver[T] class + resolve_merged defined but never instantiated in production); grep across all sidequest-server/sidequest/**/*.py finds zero production callers of Resolver() or resolve_merged(); the only production archetype path is sidequest-server/sidequest/genre/archetype/shim.py:64-158 (two-tier world-funnel-or-genre-fallback) called from sidequest-server/sidequest/server/websocket_handlers/chargen_mixin.py:391 via sidequest-server/sidequest/game/archetype_apply.py:16-23; LayeredMerge base class (resolver.py:89-128), MergeStrategy (resolver.py:23-57), and Provenance/MergeStep (protocol/provenance.py:53-76) all exist at the claimed line numbers; archetype_provenance rides the wire via protocol/models.py:194

### ADR-126 · MAJOR · Pluggable Magic System — MagicPlugin Protocol, Import-Time Registry, and Validator Severity Model
- **Claimed:** `accepted/live`
- **Finding:** The Protocol, registry dict, three plugins, validator, and YAML descriptors all exist as described. However, the critical wiring step — importing sidequest.magic.plugins to trigger the side-effect registration of all three plugins into MAGIC_PLUGINS — is absent from every production code path. Only tests/magic/conftest.py imports it. In production, MAGIC_PLUGINS is always empty, so every call to validate() in narration_apply.py:813 hits the plugin_known_but_not_registered DEEP_RED branch (validator.py:111-123) rather than running plugin-side validation. The import-time registration invariant the ADR names as its core mechanism does not fire at runtime.
- **Evidence:** sidequest-server/sidequest/magic/__init__.py:1-56 (does not import sidequest.magic.plugins); sidequest-server/sidequest/magic/plugins/__init__.py:8-10 (star-imports that fire registration); sidequest-server/sidequest/server/narration_apply.py:64,813 (imports validate and calls it); tests/magic/conftest.py:22-24 (only place that imports sidequest.magic.plugins in the entire codebase)

### ADR-8 · MINOR · Three-Tier Rule Taxonomy
- **Claimed:** `accepted/live`
- **Finding:** The `RuleTier` enum is ported and exported but is a dormant type with no production call-sites — no section is tagged with a `RuleTier`, no override logic enforces the strictness hierarchy, and the `&lt;before-you-respond&gt;` Critical-rules verification checklist in the LATE zone (the ADR's concrete mechanism) was never implemented in either the Rust or Python codebase (ADR-002 records this explicitly). The `&lt;critical&gt;` XML tag does appear in prompt strings for verbosity limits and POV rules, but as manual string wrapping, not driven by the `RuleTier` taxonomy system the ADR describes.
- **Evidence:** sidequest-server/sidequest/agents/prompt_framework/types.py:85 — RuleTier enum (Critical/Firm/Coherence) is defined and re-exported in __init__.py, but has zero production call-sites (grep of entire sidequest/ tree shows only definition + export). docs/adr/002-soul-principles.md:24 explicitly documents: "The original 2026-03-25 design called for a dual-zone EARLY+LATE pattern with a `&lt;before-you-respond&gt;` verification block; that LATE-zone block was never implemented (in either the Rust or Python tree)." A global search for "before-you-respond" and "before_you_respond" across all .py, .md, and .yaml files in production code yields zero hits in any non-ADR/non-test file. The genre pack rule override by tier (Critical can only be made stricter, Firm replaced, Coherence removed) has no corresponding loader mechanism in sidequest/genre/loader.py or sidequest/agents/orchestrator.py.

### ADR-016 · MINOR · Three-Mode Character Creation
- **Claimed:** `accepted/live`
- **Finding:** ADR-016 claims three distinct creation modes (Menu = archetype list, Guided = scene FSM, Freeform = prose→LLM extraction) all producing the same Character output, with a keyword-heuristic fallback when Claude is unavailable. In practice only the Guided (scene-based) mode is implemented; the Menu mode (zero-LLM archetype list selection) and the Freeform mode (prose input with LLM extraction and keyword-heuristic fallback) have no code at all. The `story_autogen` path is a pure table-roll backstory helper within the scene FSM, not a separate creation mode.
- **Evidence:** sidequest-server/sidequest/game/builder.py:840-975 (CharacterBuilder — scene-state-machine only, no mode selection); sidequest-server/sidequest/handlers/character_creation.py:95-117 (phase dispatch: scene/continue/confirmation/arrange_*/story_* — no menu or freeform-LLM phase); sidequest-server/sidequest/server/websocket_handlers/chargen_mixin.py:289-312 (story_autogen is pure table roll via autogen_backstory, no LLM call); no files matching chargen_mode/MenuMode/FreeformParser anywhere in production code

### ADR-068 · MINOR · Magic Literal Extraction — Domain-Scoped Constants
- **Claimed:** `accepted/live`
- **Finding:** The ADR's implementation plan calls for three new TypeScript constant files (src/constants/timing.ts, audio.ts, network.ts), a new Python sidequest_daemon/media/defaults.py for ACE-Step pipeline knobs, and a Python equivalent of the Rust sidequest-agents/src/models.rs — none of these files exist. The original duplicate literals (DEFAULT_FADE_MS in two places, inline disposition=-20 and probability thresholds in encountergen, ACE-Step pipeline parameters) remain unchanged. The only concrete pieces that landed are DEFAULT_SOCKET_PATH in daemon_client/client.py and internally-scoped constants in zimage_config.py, which is partial and predates or is orthogonal to the ADR's explicit scope.
- **Evidence:** sidequest-ui/src/audio/Crossfader.ts:1 (DEFAULT_FADE_MS=3000 inline, no src/constants/ dir exists); sidequest-ui/src/hooks/useAudioCue.ts:5 (let crossfadeMs = 3000 — duplicate still present); sidequest-ui/src/constants/ does not exist; sidequest-daemon/sidequest_daemon/media/defaults.py does not exist; sidequest-server/sidequest/agents/models.py does not exist; sidequest-server/sidequest/cli/encountergen/encountergen.py:313,401 (disposition=-20 and rng.randrange(10) < 7 still inline). Partial: sidequest-server/sidequest/daemon_client/client.py:32 defines DEFAULT_SOCKET_PATH; sidequest-daemon/sidequest_daemon/media/zimage_config.py uses internal named constants (_TURBO_STEPS, _TURBO_GUIDANCE, etc.) but is the Z-Image config, not the ACE-Step defaults.py the ADR specifies.

## Unrecorded implementation — claimed partial/drift/deferred, code is actually live (24)

### ADR-053 · MAJOR · Scenario System (Clue Graph, Belief State, Gossip Propagation)
- **Claimed:** `accepted/partial`
- **Finding:** The ADR's own implementation-status section (dated 2026-05-02) declares GossipEngine, AccusationEvaluator, and ClueGraph prerequisite enforcement as "dark," but all three now exist in production code: gossip_engine.py implements the full two-phase snapshot-then-mutate propagation, accusation.py implements AccusationEvaluator/EvidenceSummary and is wired via scenario_accusation.py into the WebSocket handler, and discover_clue() in scenario_state.py enforces DAG prerequisites and raises PrerequisiteNotSatisfiedError. The one genuinely remaining gap is that GossipEngine has no per-turn caller — it is implemented but not driven between turns (no non-test import of GossipEngine exists outside gossip_engine.py itself and political_engine.py's docstring reference).
- **Evidence:** sidequest-server/sidequest/game/gossip_engine.py:116 (GossipEngine class, 316 LOC, two-phase propagation live); sidequest-server/sidequest/game/accusation.py:135 (AccusationEvaluator.evaluate, EvidenceSummary, Circumstantial/Strong/Airtight); sidequest-server/sidequest/server/dispatch/scenario_accusation.py:87 (AccusationEvaluator wired into websocket_session_handler.py:1364); sidequest-server/sidequest/game/scenario_state.py:163-179 (discover_clue enforces DAG prerequisites, raises PrerequisiteNotSatisfiedError); sidequest-server/sidequest/game/session.py:800-803 (GossipEngine still noted as deferred in per-turn wiring comment)

### ADR-055 · MAJOR · Room Graph Navigation
- **Claimed:** `accepted/partial`
- **Finding:** The ADR's 2026-05-28 amendment calls per-transition resource depletion and trope-tick "still unwired," but both are now implemented in narration_apply.py:2226-2285 and wired at line 2525. Separately, the ADR's "Live" content claim (four worlds shipping rooms.yaml) is entirely wrong: only one caverns_and_claudes world exists (beneath_sunden) and it deliberately sets navigation_mode: region, meaning no live world actually exercises the room_graph path end-to-end.
- **Evidence:** sidequest-server/sidequest/server/narration_apply.py:2226-2285 (_apply_room_graph_transition_effects with uses_remaining decrement and OTEL span, wired at line 2525); sidequest-server/sidequest/server/websocket_handlers/chargen_mixin.py:950 (init_room_graph_location call — ADR's 2026-05-02 note incorrectly cites websocket_session_handler.py:1207-1230); sidequest-content/genre_packs/caverns_and_claudes/worlds/beneath_sunden/cartography.yaml:14 (navigation_mode: region — no world in the repo uses room_graph); grep returns zero hits for dungeon_survivor, grimvault, horden, mawdeep worlds

### ADR-100 · MAJOR · Journal Pipeline Coherence — Footnotes, KnownFacts, JOURNAL_RESPONSE, and the Scenario Clue Hook
- **Claimed:** `accepted/partial`
- **Finding:** The ADR claims `partial` with Seams A+B (story 50-5) as dark and Seam C (50-14/15/16/17) as future work, but all five named feeder stories are fully implemented and wired in production code. Seam A+B (`consume_clue_footnotes`) is called in the narrator turn pipeline at websocket_session_handler.py:1355; Seam C is complete with a live `JournalRequestHandler` registered at the dispatch table and `useStateMirror.ts` consuming `JOURNAL_RESPONSE` while dropping footnotes without `fact_id` rather than fabricating synthetic ids; `KnownFact.confidence` is already a typed `Literal` (story 50-17). The ADR's cited line numbers (orchestrator.py:683, websocket_session_handler.py:2900-2937, useStateMirror.ts:186/194) are also stale — all those paths have moved or been replaced.
- **Evidence:** sidequest-server/sidequest/server/dispatch/scenario_clue_intake.py:34-89 (Seams A+B fully implemented); sidequest-server/sidequest/server/websocket_session_handler.py:1351-1359 (consume_clue_footnotes wired); sidequest-server/sidequest/handlers/journal_request.py:1-229 (JOURNAL_REQUEST handler, Seam C); sidequest-server/sidequest/server/websocket_session_handler.py:422,440 (JOURNAL_REQUEST wired to dispatch table); sidequest-ui/src/hooks/useStateMirror.ts:43-46,271-279,145-180 (no synthetic id manufacture, JOURNAL_RESPONSE consumer live); sidequest-server/sidequest/game/character.py:41 (confidence promoted to Literal["Certain","Suspected","Rumored","Discovered"], story 50-17 complete)

### ADR-111 · MAJOR · Recency-Zone Narrator Guardrails Migrate to Tool Descriptions and Primacy-Cached Output Prose
- **Claimed:** `accepted/deferred`
- **Finding:** The ADR claims implementation-status=deferred (sprint/current-sprint.yaml#57-4), but all four guardrails are fully migrated in production code: LOCATION_PATCH_CONSTRAINT is live in the apply_world_patch tool description, NPC_INTRO and NPC_EXTRACTION guardrails are in output_only.md (formerly output_only_sdk.md, renamed in story 61-9), and CONFRONTATION_TRIGGER_CORE is composed into intent_router.py's _SYSTEM_PROMPT. The _maybe_register_legacy_guardrail gate and narrator.recency_guardrails_skipped OTEL span are both wired. The only minor gap from the ADR's schema: the live span omits the tool_backend: bool attribute (dropped post-story-61-9 when the legacy path was retired), but this is a planned simplification documented in the orchestrator comment at line 2560.
- **Evidence:** sidequest-server/sidequest/agents/narrator_guardrails.py:1-242 (all four constants + ALL_GUARDRAILS tuple); orchestrator.py:1598-1638 (_maybe_register_legacy_guardrail gating on ToolingLlmClient); orchestrator.py:2386-2553 (all four _maybe_register_legacy_guardrail call sites); orchestrator.py:2566-2573 (narrator.recency_guardrails_skipped span live); tools/apply_world_patch.py:70,147 (LOCATION_PATCH_CONSTRAINT in tool description); narrator_prompts/output_only.md:155-170 (npc_extraction guardrail); narrator_prompts/output_only.md:179-187 (npc_intro_visual guardrail); intent_router.py:40,144 (CONFRONTATION_TRIGGER_CORE in IntentRouter _SYSTEM_PROMPT); tools/_retired/begin_confrontation.py (breadcrumb confirming retirement)

### ADR-117 · MAJOR · Pluggable Ruleset Module System — Per-Genre Resolution Behind a RulesetModule Seam
- **Claimed:** `accepted/partial`
- **Finding:** The ADR's "Live" section claims only `native` and `swn` modules exist, and lists `bx`, Fate, PbtA, and 5e as "designed in spec but not implemented." In reality the registry has four fully implemented modules: `cwn` (Cities Without Number, with its own OTEL telemetry spans, Shock/Trauma/Mortal Injury resolution) and `wwn` (Worlds Without Number) are both wired and bound to live genre packs (`neon_dystopia` and `elemental_harmony` respectively). The core seam, registry, and loader integration all work exactly as the ADR describes — the gap is purely that two additional modules shipped without updating the ADR's implementation-status section.
- **Evidence:** sidequest-server/sidequest/game/ruleset/registry.py:4-14 — registry imports and registers four modules (native, swn, cwn, wwn); sidequest-server/sidequest/game/ruleset/cwn.py:35 slug="cwn"; sidequest-server/sidequest/game/ruleset/wwn.py:57 slug="wwn"; sidequest-content/genre_packs/neon_dystopia/rules.yaml:13 ruleset: cwn; sidequest-content/genre_packs/elemental_harmony/rules.yaml:16 ruleset: wwn; sidequest-server/sidequest/genre/loader.py:1522-1524 fail-loud binding confirmed live

### ADR-118 · MAJOR · Universal Retrieval Layer — Index + Per-Turn Floor-and-Fill Retrieval for NPCs, Locations, and Factions
- **Claimed:** `accepted/deferred`
- **Finding:** The ADR claims implementation-status=deferred (design-only, stories 75-4 through 75-8 not yet started), but all four implementation stories are fully live and end-to-end wired: EntityCard+projectors (75-4), retrieve_turn_context floor+fill with full OTEL span (75-5), sync_entity_cards dirty-flag reproject (75-6), and the watcher-emission dispatch wrapper (75-7). The retrieval is called every narrator turn in both websocket_session_handler.py and handlers/player_action.py, and the fill sections are injected into the Valley attention zone in orchestrator.py. Only 75-8 (a named wiring test) is unverified as a specific test file, but the production wiring it would verify is demonstrably present.
- **Evidence:** sidequest-server/sidequest/game/entity_card.py:85 (EntityCard model + per-type projectors); sidequest-server/sidequest/game/entity_store.py:30 (EntityStore typed index); sidequest-server/sidequest/game/retrieval_orchestration.py:159 (retrieve_turn_context floor+fill); sidequest-server/sidequest/game/entity_sync.py:78 (sync_entity_cards); sidequest-server/sidequest/server/dispatch/universal_retrieval.py:46 (retrieve_for_turn watcher emission); sidequest-server/sidequest/server/session_state.py:245 (entity_store on _SessionData); sidequest-server/sidequest/server/websocket_session_handler.py:2500-2505 (entity_retrieval called and fed to _build_turn_context); sidequest-server/sidequest/handlers/player_action.py:495-501 (entity_retrieval called in player action handler); sidequest-server/sidequest/agents/orchestrator.py:2143-2156 (retrieved_entity_npcs/locations/factions injected into Valley zone)

### ADR-009 · MINOR · Attention-Aware Prompt Zones
- **Claimed:** `accepted/live`
- **Finding:** The core ADR decision (zone-ordered assembly with high-attention content at primacy and recency) is fully live and wired in production via PromptRegistry.compose/compose_split/compose_split_by_zone. However, the implementation is more evolved than the ADR describes: the 3-zone model (Early/Valley/Late) has been silently expanded to 5 zones (Primacy, Early, Valley, Late, Recency), and the `weight: f32` field the ADR attributes to PromptSection was never implemented — PromptSection has no weight field and genre packs cannot adjust section weights.
- **Evidence:** sidequest-server/sidequest/agents/prompt_framework/types.py:13-68 — AttentionZone has 5 values (Primacy, Early, Valley, Late, Recency), not 3 (Early, Valley, Late) as the ADR describes; types.py:99-143 — PromptSection has no `weight: f32` field; sidequest-server/sidequest/agents/prompt_framework/core.py:137-152 — compose() sorts by zone.order() and is wired in orchestrator.py:2768 (legacy path) and 3691/3777 (SDK path)

### ADR-011 · MINOR · World State JSON Patches
- **Claimed:** `accepted/live`
- **Finding:** The core ADR-011 decision is fully live and substantially richer than described: WorldStatePatch with field-aware merge semantics exists in production code, apply_world_patch is wired as a narrator tool, and every invocation emits OTEL spans. However, two fields from the ADR's patch table — narrative_log_entry (Append) and character_patches (Merge by character name) — do not exist on WorldStatePatch; narration appends to a separate NarrativeEntry/narrative_log structure, and character mutations flow through hp_changes or typed tools. Additionally, the schema has grown significantly beyond the nine ADR-listed fields (atmosphere, current_region, pc_region, discovered_regions, hp_changes, discovered_facts, etc.) with no ADR update recording this expansion.
- **Evidence:** sidequest-server/sidequest/game/session.py:421 (WorldStatePatch class), :1248 (apply_world_patch method), :1262 (_apply_world_patch_inner), :1450-1516 (lore_established extend, npc_attitudes merge, npcs_present merge, active_stakes replace); sidequest-server/sidequest/agents/tools/apply_world_patch.py:1-214 (escape-hatch tool live in narrator tool registry); sidequest-server/sidequest/telemetry/spans/state_patch.py:16 (SPAN_APPLY_WORLD_PATCH)

### ADR-018 · MINOR · Trope Engine
- **Claimed:** `accepted/drift`
- **Finding:** The ADR frontmatter still reads `implementation-status: drift` but the ADR's own implementation notes (lines 79-85) explicitly declare "Remaining gaps: None — all four pillars are wired" after Story 45-27 and Story 50-4. Production code confirms all four pillars live: passive rate_per_turn in trope_tick.py, rate_per_day time-skip in trope_time_skip.py, beats_fired dedup counter, and dormant→progressing→resolved lifecycle — all wired at _execute_narration_turn. The only real drift is the lifecycle naming (4-state ADR text vs 3-state code, acknowledged in the ADR itself at line 89), and the stale `drift` frontmatter label which contradicts the ADR's own body text.
- **Evidence:** sidequest-server/sidequest/game/trope_tick.py:1-446 (full engine: Pass A rate_per_turn, Pass A2 time-skip, beat dedup via beats_fired counter, dormant→progressing→resolved lifecycle, OTEL spans); sidequest-server/sidequest/game/trope_time_skip.py:72 (_pass_a2_time_skip with rate_per_day); sidequest-server/sidequest/server/websocket_session_handler.py:1077-1084 (tick_tropes wired in _execute_narration_turn with days_advanced); sidequest-server/sidequest/game/session.py:461 (TropeState, status: str = "dormant"); sidequest-server/sidequest/genre/models/tropes.py:11,29,42 (TropeEscalation, PassiveProgression, TropeDefinition); docs/adr/018-trope-engine.md:11 (frontmatter: implementation-status: drift); docs/adr/018-trope-engine.md:79-81 ("Remaining gaps: None — all four pillars are wired")

### ADR-020 · MINOR · NPC Disposition System
- **Claimed:** `accepted/live`
- **Finding:** The ADR's own 2026-05-02 status section declares three things missing: a central Attitude enum, threshold-crossing OTEL detection, and qualitative-only exposure to agents. All three are now fully implemented — Attitude StrEnum + Disposition class live in disposition.py, SPAN_DISPOSITION_SHIFT emits before_attitude/after_attitude/crossed for the GM panel, narrator prompts call .attitude().value throughout, and the "Future" genre-configurable thresholds (Story 50-13) are also wired via configure_attitude_thresholds in the genre loader. The ADR's implementation-status section is stale (describes P1 RESTORE work that has since shipped).
- **Evidence:** sidequest-server/sidequest/game/disposition.py:95-108 (Attitude StrEnum, Disposition class with .attitude()); sidequest-server/sidequest/telemetry/spans/disposition.py:14-33 (SPAN_DISPOSITION_SHIFT with before_attitude/after_attitude/crossed); sidequest-server/sidequest/game/session.py:1475-1496 (threshold-crossing detection in OTEL); sidequest-server/sidequest/agents/prompt_framework/core.py:568 (narrator sees attitude, not raw int); sidequest-server/sidequest/genre/loader.py:1555 (configure_attitude_thresholds wired in load_genre_pack); sidequest-server/sidequest/genre/models/rules.py:1000 (disposition_thresholds in RulesModel)

### ADR-033 · MINOR · Genre Mechanics Engine — Confrontations & Resource Pools
- **Claimed:** `accepted/partial`
- **Finding:** Pillars 1 and 2 are live exactly as the 2026-05-02 status section claims. The ADR incorrectly marks Pillar 3 Steps 1-3 (mood_aliases alias-chain fallback) as dead data — commit 119e347c on 2026-05-18 shipped `audio/library_backend.py:resolve_mood_to_track_key` which fully implements the alias chain with OTEL spans, making Pillar 3 now completely live. Additionally, the ADR's content path `genre_workshopping/heavy_metal/audio.yaml:94` is stale — that staging tree was retired 2026-06-03 and the file now lives at `sidequest-content/genre_packs/heavy_metal/audio.yaml:114`.
- **Evidence:** sidequest-server/sidequest/audio/library_backend.py:32-77 (resolve_mood_to_track_key — ADR-033 Pillar 3 Steps 1-3 fully live, committed 2026-05-18 in feat(50-9) #336); sidequest-server/sidequest/game/encounter.py:142 (StructuredEncounter); sidequest-server/sidequest/game/resource_pool.py:114 (ResourcePool); sidequest-server/sidequest/game/session.py:855 (resources: dict[str, ResourcePool]); sidequest-server/sidequest/server/dispatch/encounter_lifecycle.py:1326-1338 (mint_threshold_lore pipeline); sidequest-server/sidequest/game/ruleset/native.py:1-10 (NativeRulesetModule, "This is ADR-033's confrontation engine, relocated")

### ADR-059 · MINOR · Monster Manual — Server-Side Pre-Generation via Game-State Injection
- **Claimed:** `accepted/live`
- **Finding:** The ADR's embedded implementation-status section (written 2026-05-02) declared MonsterManual, seeding pipeline, compound-key lookup, post-narration gate, and encountergen as dark — but all of these are now fully implemented and wired in production code. The frontmatter `implementation-status: live` accurately reflects current code reality for the core pipeline (game_state injection + pregen seeding + mark-active gate); the embedded status note inside the ADR is now stale and understates what has been built. The only genuine gap is `loadoutgen`, which remains a 1-line placeholder stub with an explicit OTEL flag `tool.loadout.loadoutgen_wired=False` in generate_loadout.py.
- **Evidence:** sidequest-server/sidequest/server/websocket_session_handler.py:695-733 (ensure_loaded + inject wired), :918-933 (mark_active_from_narration + save wired); sidequest-server/sidequest/server/dispatch/monster_manual_inject.py:60-107 (ensure_loaded with seed_manual call); sidequest-server/sidequest/server/dispatch/pregen.py:183-341 (seed_manual fully implemented); sidequest-server/sidequest/cli/encountergen/encountergen.py:672-720 (main() fully implemented ~720 LOC); sidequest-server/sidequest/game/monster_manual.py:88-368 (MonsterManual class with all methods); sidequest-server/sidequest/agents/tools/generate_loadout.py:86-94 (loadoutgen still explicitly dark)

### ADR-061 · MINOR · Lore Module Decomposition — Split lore.rs by Responsibility
- **Claimed:** `accepted/live`
- **Finding:** The ADR's post-port mapping section (lines 101-112) names three Python modules — lore_store.py, lore_seeding.py, lore_embedding.py — and explicitly states that "accumulator methods live on LoreStore in lore_store.py." In reality a fourth module, lore_accretion.py, was created to host the accumulation responsibility (accrete_facts_to_lore), and it is fully wired into production dispatch. The language/conlang knowledge responsibility (language.rs in the original Rust plan) has no Python equivalent in any lore module — it either moved elsewhere or was dropped.
- **Evidence:** sidequest-server/sidequest/game/lore_accretion.py:1-116 (exists, wired via sidequest-server/sidequest/server/dispatch/lore_accretion.py:21 and websocket_session_handler.py:3008); lore_store.py:143, lore_seeding.py:53, lore_embedding.py:276 all exist and are imported in production code; ADR post-port mapping (lines 101-112) names only three modules and claims accumulator methods live on LoreStore in lore_store.py

### ADR-064 · MINOR · Game Crate Domain Modules — Organize 69 Flat Files
- **Claimed:** `accepted/live — post-port amendment (2026-04-23) states the Python port landed with `sidequest-server/sidequest/game/` mostly flat, with `projection/` being the one domain that grew to directory scope.`
- **Finding:** The 2026-04-23 post-port amendment claims `projection/` is the sole domain that grew to directory scope, but the current tree has four additional domain subdirectories (`pg/`, `ruleset/`, `cookbook/`, `table/`) — each with multiple files and an `__init__.py`. The core decision (prefer flat, anti-overnesting, allow selective promotion) is being correctly followed, but the amendment's description of the current state ("one domain") is stale relative to organic growth after the port.
- **Evidence:** sidequest-server/sidequest/game/ has 5 domain subdirectories beyond the root: projection/ (13 files), pg/ (10 files), ruleset/ (7 files), cookbook/ (7 files), table/ (6 files). ADR line 179 claims "projection/ is the one domain that did grow to directory scope." There are 90 flat files at root plus 48 files across those 5 subdirectories.

### ADR-70 · MINOR · MLX Image Renderer — Replace PyTorch/diffusers with Apple MLX
- **Claimed:** `accepted/live`
- **Finding:** The ADR's core decision (drop PyTorch/diffusers, drop TTSWorker, isolate ACE-Step, add MLX) is fully implemented and live. However, the implementation went further than the ADR describes: the migration plan names `FluxMLXWorker` as the Phase 2 class and `renderer_factory.py` as the update target, but the actual production code landed as `ZImageMLXWorker` (using the Z-Image model via mflux, not Flux), and `renderer_factory.py` was never created — WorkerPool in daemon.py directly constructs the worker. ACE-Step also remains in the daemon as `AceStepAdapter`/`MusicPipeline` for operator-triggered music generation (consistent with the ADR's intent, but the ADR implied it would be entirely removed from the daemon).
- **Evidence:** sidequest-daemon/pyproject.toml:7-15 (mlx>=0.18, mflux>=0.17.5; no torch/diffusers/onnxruntime/piper); sidequest-daemon/sidequest_daemon/media/workers/zimage_mlx_worker.py:1-6 (docstring: "Replaces FluxMLXWorker"); sidequest-daemon/sidequest_daemon/media/daemon.py:312-320 (WorkerPool.warm_up_image imports ZImageMLXWorker, not FluxMLXWorker); sidequest-daemon/CLAUDE.md line ~2 ("The Flux MLX worker has been retired — zimage_mlx_worker.py is the sole runtime image worker"); no renderer_factory.py exists anywhere in sidequest_daemon/

### ADR-087 · MINOR · Post-Port Subsystem Restoration Plan
- **Claimed:** `accepted/live`
- **Finding:** The ADR's claim that `implementation-status: live` refers to the document itself as a meta-tracker is accurate, but several concrete mechanisms it describes are either more complete (mood_aliases chain consumer IS wired in `audio/library_backend.py`, contrary to the P3 "no consumer fires the chain" assertion) or still missing (loadoutgen remains a placeholder stub with `loadoutgen_wired=False` in production code, and neither namegen nor encountergen are registered as script entry points in pyproject.toml despite being invoked in-process via pregen.py). The DEV_SCENES env var gating described in the F-section for ADR-092 has been removed — app.py now unconditionally registers the scene harness router with a comment "former DEV_SCENES env var added zero security value."
- **Evidence:** sidequest-server/sidequest/server/dispatch/pregen.py:1-240 (P0 pregen dispatch live, in-process via encountergen_main/namegen_main); sidequest-server/sidequest/server/app.py:304-311 (DEV_SCENES env var removed, scene harness unconditionally wired); sidequest-server/sidequest/audio/library_backend.py:53-65 (mood_aliases chain consumer live with OTEL spans — ADR P3 claim says "no consumer fires the chain"); sidequest-server/sidequest/cli/loadoutgen/__init__.py:1 (still placeholder stub); sidequest-server/sidequest/agents/tools/generate_loadout.py:94 (loadoutgen_wired=False explicit); sidequest-server/pyproject.toml:34-35 (only sidequest-server entry point; namegen/encountergen/loadoutgen not registered as scripts); sidequest-server/sidequest/game/disposition.py:95 (Attitude enum live); sidequest-server/sidequest/game/gossip_engine.py:116 (GossipEngine live); sidequest-server/sidequest/game/accusation.py:131 (AccusationEvaluator live); sidequest-ui/src/App.tsx:1001 (MAP_UPDATE handler at line 1001, not 781 as ADR states)

### ADR-88 · MINOR · ADR Frontmatter Schema and Auto-Generated Indexes
- **Claimed:** `accepted/live`
- **Finding:** The ADR claims implementation-status: live and the core deliverables are fully present: all three scripts exist, all ADRs have been migrated to frontmatter, and all four generated views (README.md, CLAUDE.md block, DRIFT.md, SUPERSEDED.md) are auto-generated with proper markers. However, implementation plan step 6 ("Add validate_adr_frontmatter.py to pre-commit hooks") was never completed — no active pre-commit hook exists and CI has no ADR validation job — meaning the "validator enforced by hook + CI" guarantee the ADR makes is not enforced. The TEMPLATE.md for new contributors is also absent. The verdict is unrecorded-impl rather than overstated because the frontmatter status says "live" and the core machinery genuinely is live; the hook/CI gap is an unfinished sub-step of the implementation plan rather than a false claim about the main decision.
- **Evidence:** scripts/regenerate_adr_indexes.py (555 lines), scripts/validate_adr_frontmatter.py (234 lines), scripts/migrate_adr_frontmatter.py (334 lines) all exist; all 137 ADR .md files have YAML frontmatter; docs/adr/DRIFT.md and docs/adr/SUPERSEDED.md are generated and populated; CLAUDE.md lines 268/338 and docs/adr/README.md line 60 have ADR-INDEX:GENERATED markers; however .git/hooks/pre-commit does not exist (only .sample files), no CI workflow references validate_adr_frontmatter, and docs/adr/TEMPLATE.md is absent.

### ADR-096 · MINOR · Cavern Renderer Revival — Pre-Rendered Cellular Caverns for Tactical Maps
- **Claimed:** `accepted/partial`
- **Finding:** ADR-096 is marked partial but the full format contract and end-to-end pipeline are live: the cellular automaton (gen_cave) is implemented and wired into the generator registry, TacticalGridPayload (mask + cavern_image_url + CellularParams) is the production protocol model, _maybe_emit_tactical_grid fires on room entry in websocket_session_handler, TacticalGridRenderer.tsx uses image-mode (no SVG floor-cell paths remain), and Automapper.tsx routes cavern rooms through it in production. The ADR's stated "partial" status reflects the original authoring-pipeline intent (24 committed rooms for caverns_sunden), but the ADR-106 amendment redirected to runtime generation — which is also fully live in sidequest/dungeon/materializer.py with mask_sha OTEL emission.
- **Evidence:** sidequest-server/sidequest/dungeon/interiors/cellular.py:1-41 (gen_cave live); sidequest-server/sidequest/dungeon/interiors/generator.py (gen_cave wired into generator registry); sidequest-server/sidequest/protocol/models.py:516-791 (TacticalGridPayload with mask, cavern_image_url, CellularParams, DerivedRoomData live); sidequest-server/sidequest/server/websocket_handlers/map_emit.py:5-180 (_maybe_emit_tactical_grid production handler); sidequest-server/sidequest/server/websocket_session_handler.py:1899-1901 (called on room enter); sidequest-ui/src/components/TacticalGridRenderer.tsx:17-123 (image-mode renderer, no SVG floor-cell paths); sidequest-ui/src/components/Automapper.tsx:347-351 (TacticalGridRenderer wired in production); sidequest-ui/src/lib/cellMath.ts (chebyshevReachCells); sidequest-server/sidequest/dungeon/materializer.py:347-414 (mask_sha computed and persisted)

### ADR-105 · MINOR · Broadcast-Layer Perception Firewall — Completing ADR-104 in the MP Fan-Out
- **Claimed:** `accepted/partial`
- **Finding:** The ADR frontmatter still reads implementation-status: partial, but the 2026-05-28 amendment body explicitly declares "Both tracks have landed" and the code fully confirms it: Track A (per-row emitter authorship via author_player_id), Track B (private_prose_segments field on NarrationTurnResult, derived visible_to in visibility_classifier, visibility-gated CoreInvariant for SECRET_NOTE/NARRATION_SEGMENT, NARRATION_SEGMENT fan-out in websocket_session_handler, structural prompt redaction, retired pronoun passes in pov_swap) are all wired in production code. The only gap is that the YAML frontmatter was never updated from partial to complete after the amendment was appended.
- **Evidence:** emitters.py:317-397 (emit_event with author_player_id Track A); orchestrator.py:485 (private_prose_segments field), 3229,3538 (populated on both backends); visibility_classifier.py:85,163-193 (classify_narration_visibility reads secret_routes, derives private_segments, no hardcoded "all"); invariants.py:49-71,141-176 (SECRET_NOTE removed from TARGETED_KINDS, visibility-gated branch reads _visibility.visible_to); websocket_session_handler.py:1414-1495 (NARRATION_SEGMENT emitted per private segment with author_player_id=_owner_pid); session_handler.py:34-35,69 (NarrationSegmentPayload/NarrationSegmentMessage registered); prompt_redaction.py:27-93 (redact_dispatch_package structural redaction + prompt.redaction.structural OTEL span); pov_swap.py:412-427 (Passes 5/6/7 retired comment); protocol/messages.py:126-130,898-902 (NarrationSegmentPayload/NarrationSegmentMessage defined)

### ADR-109 · MINOR · Persistent Location Descriptions + Mechanical Manifest
- **Claimed:** `accepted/partial`
- **Finding:** The ADR claims partial implementation but all stories (54-1 through 54-9 and 55-1) are substantially live: the two-mode resolver, location_promotions table, LOCATION_DESCRIPTION WebSocket message, pf validate locations CLI, LocationPanel.tsx, encounter overlays (entity_delta/prose_suffix), OTEL spans, compose_room_prose for procedural worlds, and content backfills for glenross are all present in production code. The one genuine stale-info is that ADR-109 §4 describes "a new location_promotions SQLite table keyed by (save_id, region_id, entity_id)" — the actual substrate is PostgreSQL (ADR-115 complete migration), keyed by (session_id, region_id, entity_id), with the PgPromotionStore adapter documenting the save_id→session_id key change explicitly.
- **Evidence:** sidequest-server/sidequest/agents/tools/resolve_location_entity.py:124 (resolver tool live); sidequest-server/sidequest/game/location_resolver.py:224 (two-mode resolve() live); sidequest-server/sidequest/game/pg/promotions.py:68 (PgPromotionStore with PostgreSQL session_id key, not SQLite save_id); sidequest-server/alembic/versions/0001_initial_unified_schema.py:149 (location_promotions DDL in Postgres); sidequest-server/sidequest/protocol/enums.py:117 (LOCATION_DESCRIPTION message type); sidequest-server/sidequest/server/websocket_handlers/map_emit.py:347 (_maybe_emit_location_description live); sidequest-server/sidequest/cli/validate/locations.py:1 (pf validate locations CLI); sidequest-ui/src/components/LocationPanel.tsx:41 (LocationPanel.tsx UI); sidequest-server/sidequest/telemetry/spans/location.py:153 (location_entity_resolve_span); sidequest-content/genre_packs/tea_and_murder/worlds/glenross/cartography.yaml:37 (entities[] backfill present); ADR-109 §4 claims "(save_id, region_id, entity_id) SQLite table" but code uses (session_id, region_id, entity_id) in PostgreSQL

### ADR-113 · MINOR · Intent Router — Mechanical-Engagement Spine
- **Claimed:** `accepted/partial`
- **Finding:** The 2026-05-28 amendment explicitly states "The §Confidence gate is NOT implemented — it is deferred" and claims SubsystemDispatch has no confidence field and run_dispatch_bank executes every dispatch unconditionally. However, Story 71-16 (completed 2026-05-28, same day) implemented exactly the specified confidence gate: SubsystemDispatch now has a required confidence float field, run_dispatch_bank gates each dispatch against a per-subsystem threshold (default 0.6), reads pack-level overrides from RulesConfig.dispatch_confidence_thresholds, and degrades below-threshold dispatches to narrator hints. The spine itself (IntentRouter, execute_intent_router_pre_narrator_pass, run_dispatch_bank, dispatch_engagement_watcher) is all live as the amendment confirms; the amendment's claim that confidence gating is deferred is the sole inaccuracy.
- **Evidence:** sidequest-server/sidequest/protocol/dispatch.py:97-111 (SubsystemDispatch.confidence field, required, ge=0.0 le=1.0); sidequest-server/sidequest/agents/subsystems/__init__.py:62-87 (DEFAULT_DISPATCH_CONFIDENCE_THRESHOLD=0.6, _threshold_for() reading pack rules.yaml); sidequest-server/sidequest/agents/subsystems/__init__.py:277-302 (if d.confidence < threshold: → degrade to narrator hint, else engage); sidequest-server/sidequest/genre/models/rules.py:1067 (RulesConfig.dispatch_confidence_thresholds field); sprint/epic-71.yaml:177-188 (Story 71-16 status: done, completed 2026-05-28)

### ADR-123 · MINOR · Mechanical-Engagement Pipeline — Confidence-Gated Topological Dispatch Bank, Precondition/Unregistered Gates, and the LethalityArbiter
- **Claimed:** `accepted/live`
- **Finding:** All five pipeline stages are fully implemented and wired exactly as described: unregistered gate → precondition gate → topo-sorted confidence-gated bank → LethalityArbiter → narrator directives. The ADR's snapshot is slightly stale in two places: it states "the only entry today is scenario_clue" in _INERT_PRECONDITIONS (code has a second entry for "witnessed_act"), and it lists "seven live handlers" in the dispatch bank registry (code has nine, adding "witnessed_act" and "equip"). These additions are consistent with the ADR's design and represent later work not documented here.
- **Evidence:** sidequest-server/sidequest/agents/dispatch_precondition_gate.py:84-87 (_INERT_PRECONDITIONS has both "scenario_clue" and "witnessed_act", not just scenario_clue as ADR states); sidequest-server/sidequest/agents/subsystems/__init__.py:167-193 (_register_defaults registers 9 handlers — confrontation, magic_working, scenario_clue, reflect_absence, distinctive_detail_hint, npc_agency, movement, witnessed_act, equip — not 7 as ADR claims); sidequest-server/sidequest/server/intent_router_pass.py:287-300 (both gates wired in correct order before bank); sidequest-server/sidequest/agents/orchestrator.py:2663-2723 (Stage 4 arbiter + Stage 5 directive registration present and wired, though ADR cites :2628-2672 which is off by ~35 lines)

### ADR-135 · MINOR · Reference Pages Are a Public Table Tool — Single Fixed Projection, No GM Audience
- **Claimed:** `accepted/deferred`
- **Finding:** The ADR is marked deferred but its entire audience-doctrine decision is live: the reference routes carry zero audience/GM parameters, the visibility whitelist enforces the single public projection, and Stories 65-7 through 65-9 (r2_manifest gate, POI gallery, public Cast from portrait_manifest.yaml) are all implemented in production reference_renderer.py. The only nuance between ADR description and code is that portrait_manifest.yaml remains in EXCLUDED_FILES (never passed through the main YAML walk) but is consumed via a dedicated load_cast_entries() path — functionally equivalent to the "projection, not exclusion" doctrine but mechanically different from what the ADR describes.
- **Evidence:** sidequest-server/sidequest/server/reference_routes.py:86-133 (no audience param, single fixed public projection live); sidequest-server/sidequest/server/reference_renderer.py:471-490 (EXCLUDED_FILES: npcs.yaml stays excluded, portrait_manifest.yaml stays in EXCLUDED_FILES but read separately); sidequest-server/sidequest/server/reference_renderer.py:1210-1370 (load_cast_entries + assemble_lore_page implement Stories 65-7/65-8/65-9: portrait_manifest public Cast section, r2_manifest gate, POI gallery, all live); sidequest-server/sidequest/server/reference_visibility.py:98-120 (KEEPER set enforces no audience split); sidequest-server/sidequest/server/views.py:115-120 (confirms no is_gm() predicate)

### ADR-137 · MINOR · Quest & Stakes Substrate — Create/Anchor Lane, First-Class active_stakes Source, and One-Mechanism Consolidation
- **Claimed:** `proposed / implementation-status=deferred (preamble explicitly states "no production code lands in 77-1")`
- **Finding:** The ADR is marked fully deferred and its own preamble says "no production code lands in 77-1," but story 77-1 (seed-at-creation Option A) is fully implemented and wired: quest_seed.py contains seed_quest_spine(), chargen_mixin.py calls it at line 798, and the quest.seeded_at_creation OTEL span fires on all paths including the warning path for empty drives. Stories 77-2 through 77-7 (typed record_quest/set_stakes tools, WorldStatePatch promotion of quest_anchors, one-mechanism cleanup, UI panel, content seed deck) are genuinely absent from production code, consistent with the deferred claim.
- **Evidence:** sidequest-server/sidequest/game/quest_seed.py:36 (seed_quest_spine fully implemented); sidequest-server/sidequest/server/websocket_handlers/chargen_mixin.py:46,798 (imported and called at session creation); sidequest-server/sidequest/telemetry/spans/state_patch.py:49,147 (quest.seeded_at_creation span defined and wired). ADR preamble (line 17-20) explicitly says "no production code lands in 77-1" — contradicted by the above. Stories 77-2 through 77-7 remain genuinely absent: no record_quest/set_stakes tools (agents/tools/ directory confirmed), quest_anchors still absent from WorldStatePatch (session.py:421-452 verified), quest_updates lane still live in orchestrator.py:472,1258,3219,3549, /active_stakes still in apply_world_patch.py:116,195.

## Stale / deprecated info — code no longer matches the ADR text (30)

### ADR-012 · MAJOR · Agent Session Management
- **Claimed:** `accepted/live`
- **Finding:** ADR-012 claims the live mechanism is persistent `--resume` Claude CLI sessions per agent, but this was superseded twice: ADR-066 elaborated on sessions then ADR-098 replaced them with stateless per-turn calls (dropping --resume entirely), and ADR-101 replaced the CLI subprocess backend altogether with AnthropicSdkClient as the sole production narrator. ADR-012's `superseded-by: null` frontmatter is wrong — the decision is effectively dead, overridden by ADR-098 and ADR-101, neither of which is recorded as a successor in ADR-012's metadata.
- **Evidence:** sidequest-server/sidequest/agents/llm_factory.py:53,59-66 (default=anthropic_sdk, claude/ollama raise NarratorBackendRetired); sidequest-server/sidequest/agents/orchestrator.py:2965-2966 (ADR-098: stateless — no persistent session; current_session_id=None); sidequest-server/sidequest/agents/claude_client.py:290-341 (send_with_session with --resume exists but unreachable from production narrator path); docs/adr/066-persistent-opus-narrator-sessions.md:9 (superseded-by:98); docs/adr/098-stateless-narrator-turns.md:8-9 (supersedes:[66], status=accepted/live); docs/adr/SUPERSEDED.md:49-51 (ADR-066 superseded by ADR-098)
- **Conflicts with:** 098

### ADR-23 · MAJOR · Session Persistence
- **Claimed:** `accepted/live`
- **Finding:** ADR-023 describes storage as a single SQLite .db file per save slot (or JSON files in the Python origin), with an append-only JSONL narrative log on disk — but ADR-115 (2026-05-27, complete) retired all SQLite writes and moved state to a single shared PostgreSQL database with a `narrative_log` table, a `game_state` table, and a `sessions` table. The auto-save-after-every-turn behaviour and "Previously On..." recap generation are both still live, but the storage substrate described in ADR-023 is entirely gone. The gap is a broken successor pointer: ADR-023 has `superseded-by: null` and ADR-115 has `supersedes: []`, so the formal ADR lineage doesn't record the replacement.
- **Evidence:** docs/adr/023-session-persistence.md:44 ("Save files are self-contained (one .db file per save slot)"); sidequest-server/sidequest/game/pg/sessions.py:55-61 (Postgres `sessions` table, single shared DB); sidequest-server/sidequest/game/persistence.py:1-18 (SQLite write layer explicitly retired per ADR-115); sidequest-server/sidequest/server/websocket_session_handler.py:1187-1195 (auto-save after every turn still live, via room.save()); sidequest-server/sidequest/game/pg/snapshot.py:188-209 (_generate_recap still called on load); docs/adr/023-session-persistence.md:7-8 (superseded-by: null)
- **Conflicts with:** 115-postgres-persistence-substrate.md

### ADR-073 · MAJOR · Local Fine-Tuned Model Architecture
- **Claimed:** `accepted/live`
- **Finding:** ADR-073 claims Ollama is a selectable alternative narrator backend (via SIDEQUEST_LLM_BACKEND=ollama) and that Phase 0 live JSONL training capture fires per-turn into ~/.sidequest/training/. In reality, story 61-9 / ADR-101 retired the ollama backend — llm_factory.py raises NarratorBackendRetired for it unconditionally — and Phase 0's live JSONL-per-turn capture was never implemented; TurnRecord lacks system_prompt/response_text/game_patch fields and the corpus pipeline mines pairs retroactively from save files rather than streaming live turns to disk. The LlmClient Protocol (Phase 1) is live in claude_client.py:813 but with a different interface signature than the ADR describes.
- **Evidence:** sidequest-server/sidequest/agents/llm_factory.py:16,59-66 — `_RETIRED_BACKENDS = frozenset({"claude", "ollama"})` and `NarratorBackendRetired` is raised for both; sidequest-server/sidequest/agents/ollama_client.py:66 — OllamaClient exists but is unreachable via factory; sidequest-server/sidequest/telemetry/turn_record.py:28-64 — TurnRecord has no system_prompt/response_text/game_patch/prompt_tier fields; sidequest-server/sidequest/corpus/miner.py:30 — training pairs are mined retroactively from saves, not emitted live per turn; no ~/.sidequest/training/ JSONL path exists anywhere in production code
- **Conflicts with:** 101

### ADR-074 · MAJOR · Dice Resolution Protocol — Player-Facing Rolls via WebSocket
- **Claimed:** `accepted/live`
- **Finding:** The ADR's §Server authority model claims the server generates a cryptographic seed and determines die face results from RNG; the actual code is "physics-is-the-roll" where the client rolls physically in Rapier and reports settled face values that the server treats as authoritative. The described turn flow (server sends DiceRequest first, then awaits DiceThrow) is also inverted: the UI auto-rolls locally and sends DICE_THROW directly, and the code explicitly raises DiceDispatchError for server-initiated dice flow ("not supported in the Python port yet"). The UI dice library files named in the ADR as living in sidequest-ui/src/dice/ actually live in a separate @local/dice-lib package outside the sidequest-ui subrepo, and the cited payloads.ts line numbers are off by ~130-190 lines.
- **Evidence:** sidequest-server/sidequest/game/dice.py:1 ("physics-is-the-roll path"), dice.py:171 ("Seed no longer drives the outcome on the physics-is-the-roll path (client faces are authoritative)"), sidequest-server/sidequest/server/dispatch/dice.py:7-9 (client builds DiceRequestPayload locally, auto-rolls in Rapier, reads settled faces — no server round-trip), dispatch/dice.py:286-291 ("server-initiated dice flow is not supported in the Python port yet (UI drives all rolls via beat selection)"), /Users/slabgorb/Projects/dice-lib/src/ (d4.ts, d6.ts, d10.ts, d12.ts, d20.ts, DiceScene.tsx, useDiceThrowGesture.ts, replayThrowParams.ts, diceTheme.ts all live in dice-lib, not sidequest-ui/src/dice/)

### ADR-003 · MINOR · Genre Pack Architecture
- **Claimed:** `accepted/live`
- **Finding:** The ADR's core architectural decision is correctly implemented: load_genre_pack/load_genre_pack_cached exist and are wired in production code (rest.py:774, dispatch/pregen.py:208, interior/dispatch.py:42), models live in genre/models/, cache.py backs the cached variant, and the worlds/ split is present across all packs. However, the implementation-status section contains two stale-info items: the line numbers for both functions have drifted (load_genre_pack is at line 1215 not 753; load_genre_pack_cached is at line 1683 not 1032), and the "seven live packs" list names `victoria` which does not exist on disk — the actual 11 packs include neon_dystopia, pulp_noir, road_warrior, tea_and_murder, and wry_whimsy instead. The ADR itself acknowledges the pack list is not authoritative ("the filesystem is the source of truth"), so this is a documentation staleness rather than an architectural mismatch.
- **Evidence:** sidequest-server/sidequest/genre/loader.py:1215 (load_genre_pack, not line 753 as claimed); loader.py:1683 (load_genre_pack_cached, not line 1032 as claimed); sidequest-content/genre_packs/ has 11 packs including neon_dystopia, pulp_noir, road_warrior, tea_and_murder, wry_whimsy but NOT victoria (which the ADR lists as one of "seven live packs"); core architecture (loader, models/, cache.py, worlds/ split, mandatory YAML files) all confirmed live at correct paths.

### ADR-004 · MINOR · Lazy Genre Binding
- **Claimed:** `accepted/live`
- **Finding:** The core architectural decision is correctly implemented: server starts genre-agnostic, genre pack is loaded lazily in the connect handler when a client binds a session. However, all three specific line-number pointers in the 2026-05-02 implementation-status section are wrong: SessionEventPayload is at messages.py:352 not :180, the loader.load call is at connect.py:448 not :283, and the genre_pack attribute lives in session_state.py:195 (_SessionData dataclass) rather than session_handler.py:422 (that file is only 401 lines and has no genre_pack field).
- **Evidence:** sidequest-server/sidequest/handlers/connect.py:448 (genre_pack = loader.load, not :283 as ADR claims); sidequest-server/sidequest/protocol/messages.py:352 (SessionEventPayload, not :180 as ADR claims); sidequest-server/sidequest/server/session_state.py:195 (genre_pack: GenrePack field in _SessionData, NOT session_handler.py:422 as ADR claims); sidequest-server/sidequest/server/session_handler.py:7-8 (docstring claim correct); sidequest-server/sidequest/server/app.py:138 (only genre_pack_search_paths stored at startup, no pack loaded)

### ADR-006 · MINOR · Graceful Degradation
- **Claimed:** `accepted/live`
- **Finding:** The core decision is fully live: DaemonUnavailableError is raised and caught at the dispatch boundary, skipped_daemon_unavailable propagates through EmbedWorkerResult, and OTEL render.skipped events fire on daemon-down conditions — all confirmed in production code. However, every concrete line-number pointer in the implementation-status section has drifted: websocket_session_handler.py:3466 does not exist (file ends at 3425), AudioInterpreter moved from session_handler.py to session_state.py during the session-handler decomposition, and rest.py:456 vs :398 for has_music_director. The mechanism is correct; the internal bookkeeping pointers are stale.
- **Evidence:** DaemonUnavailableError catch: sidequest-server/sidequest/server/websocket_session_handler.py:3077 (ADR claims :3466, file is 3425 lines); skipped_daemon_unavailable field: sidequest-server/sidequest/game/lore_embedding.py:79 (line correct); render.skipped OTEL: websocket_session_handler.py:2733,3085 (ADR claims :3105/:3113); AudioInterpreter instantiated in sidequest-server/sidequest/server/session_state.py:104 not session_handler.py:128 as ADR states; has_music_director=False at sidequest-server/sidequest/server/rest.py:456 (ADR claims :398); MusicDirectorDecision at sidequest-server/sidequest/audio/protocol.py:18 (correct)

### ADR-007 · MINOR · Unified Character Model
- **Claimed:** `accepted/live`
- **Finding:** The core decision (one unified Character model, narrative-first, character.py:66) is live and correct. However, the ADR's 2026-05-02 update section claims "HP removed per ADR-014; access via Character.edge and Character.max_edge (computed from core.edge.current/core.edge.max)" — but ADR-114 reversed that: HpPool is back in CreatureCore, Character exposes hp()/max_hp()/hp_fraction() methods, and armor_class is a first-class field on CreatureCore. There is no `edge` or `max_edge` on Character or CreatureCore in the current code.
- **Evidence:** sidequest-server/sidequest/game/character.py:66 (class confirmed), :167-194 (hp/max_hp/is_broken/hp_fraction methods exist, NOT edge/max_edge); sidequest-server/sidequest/game/creature_core.py:22-44 (HpPool live), :119 (hp: HpPool field on CreatureCore), :123 (armor_class: int = 10 on CreatureCore); ADR-114 reversal noted in creature_core.py:25-26

### ADR-013 · MINOR · Lazy JSON Extraction
- **Claimed:** `accepted/drift`
- **Finding:** The architectural decision is accurately described: `extract_structured_from_response` is a live three-tier extractor in `orchestrator.py`, exported from `__init__.py`, and invoked only on the legacy `claude -p` streaming path — dead code on the default `anthropic_sdk` narrator path per ADR-102. However, the 2026-05-28 amendment's specific line-number citations are stale due to code growth (function at 1180 not 1139; CLI invocation at 3158 not 3034; additional invocations at 3413/3632 not 3289/3508), and `local_dm.py` no longer exists so the `_extract_json_object` claim at `local_dm.py:229` cannot be verified. The `implementation-status: drift` label is appropriate — the extractor is reachable in code but not exercised by the default narrator configuration.
- **Evidence:** orchestrator.py:1180 (extract_structured_from_response, ADR claims line 1139); orchestrator.py:3148,3158 (Claude CLI streaming path, ADR claims lines 3024/3034); invocations at 3413, 3632 (ADR claims 3289, 3508); __init__.py:52,99 (export confirmed accurate); local_dm.py does not exist (no _extract_json_object at claimed local_dm.py:229); llm_factory.py:12,15,53,63-66 (SIDEQUEST_LLM_BACKEND, default anthropic_sdk, warns claude/ollama non-viable)

### ADR-026 · MINOR · Client-Side State Mirror
- **Claimed:** `accepted/live`
- **Finding:** The ADR's core mechanism (state_delta on NARRATION/TURN_STATUS, slash commands client-side) is structurally present, but two mechanisms are stale: (1) the "full state sync on reconnect via SESSION_EVENT { event: 'ready', initial_state: {...} }" claim is wrong — the server hardcodes initial_state=None and uses event-log replay instead; (2) NARRATION messages always carry state_delta=None in production, with state deltas only populating on NARRATION_END (carrying location/encounter_id/magic_state but not characters/quests). The amendment's cited line numbers (:80, :318, :497) are also stale — actual lines are 88, 331, 510.
- **Evidence:** sidequest-server/sidequest/handlers/connect.py:1400 (initial_state=None on SESSION_EVENT ready); sidequest-server/sidequest/server/websocket_session_handler.py:1407 (NarrationPayload always has state_delta=None in production); websocket_session_handler.py:2038 (state_delta populated only on NARRATION_END via _shared_world_delta_to_state_delta, carries location/encounter_id/magic_state only, not characters/quests); sidequest-server/sidequest/protocol/messages.py:88,331,510 (state_delta: StateDelta | None = None on NarrationPayload, NarrationEndPayload, TurnStatusPayload); sidequest-ui/src/hooks/useStateMirror.ts:119-135 (handles initial_state if present, but receives None in practice); sidequest-ui/src/hooks/useSlashCommands.ts:24-48 (slash commands fully client-side, no server round-trip — agrees)

### ADR-027 · MINOR · Reactive State Messaging
- **Claimed:** `accepted/live`
- **Finding:** The ADR's core decision (typed WebSocket messages pushed on state change, server diffs before/after state) is live and implemented in game/delta.py and websocket_session_handler.py. However, 4 of the 5 specifically named message types are stale: CHARACTER_SHEET and INVENTORY were removed in 2026-04 and consolidated as nested facets inside PARTY_STATUS (the only remaining discrete message), COMBAT_EVENT handler was removed in story 28-9, and MAP_UPDATE was deleted from the MessageType enum in the Rust-to-Python port (it survives only as a raw wire-literal string in CartographyMapMessage, not as an official enum entry).
- **Evidence:** sidequest-ui/src/types/protocol.ts:6 ("CHARACTER_SHEET and INVENTORY removed 2026-04 — now nested inside each PartyMember carried by PARTY_STATUS"); sidequest-ui/src/App.tsx:1030 ("COMBAT_EVENT handler removed in story 28-9"); sidequest-server/sidequest/protocol/enums.py:39 (PARTY_STATUS live), enums.py:126-128 ("ADR-019 MAP_UPDATE was deleted in the Rust→Python port"); sidequest-server/sidequest/protocol/messages.py:1414 (MAP_UPDATE reused as a raw wire literal in CartographyMapMessage but absent from MessageType StrEnum); sidequest-server/sidequest/game/delta.py:145-167 (compute_delta() implements the before/after state diffing pattern)

### ADR-031 · MINOR · Game Watcher — Semantic Telemetry for AI Agent Observability
- **Claimed:** `accepted/live`
- **Finding:** The three-layer telemetry model, TurnRecord, Validator (five checks), WatcherSpanProcessor, and /ws/watcher endpoint are all fully live and wired into production code exactly as claimed. However, the ADR's Python-port note (added 2026-04-25) names "telemetry/spans.py" as a single file, but that file was subsequently refactored into a package (telemetry/spans/) — the file path in the port-note is wrong. Additionally, the "CLI tail: just watch" viewer command from the Viewer Progression section does not exist; the actual command is "just otel" which opens the GM dashboard at /dashboard.
- **Evidence:** ADR port-note (line 198): "sidequest-server/sidequest/telemetry/spans.py" — file does not exist; actual implementation is the package sidequest-server/sidequest/telemetry/spans/__init__.py (with SpanRoute, SPAN_ROUTES, FLAT_ONLY_SPANS in _core.py:23,39,44). ADR Viewer Progression (line 149): "just watch" — no such recipe in justfile; the viewer command is "just otel" (justfile:472). Core claims all confirmed live: WatcherSpanProcessor in sidequest-server/sidequest/server/watcher.py:50; Validator with five checks in sidequest-server/sidequest/telemetry/validator.py:345-362; TurnRecord in sidequest-server/sidequest/telemetry/turn_record.py:28; /ws/watcher endpoint wired in sidequest-server/sidequest/server/app.py:293; Validator started at app startup (app.py:147,151).

### ADR-037 · MINOR · Shared-World / Per-Player State Split
- **Claimed:** `accepted/live`
- **Finding:** The ADR's core decision — canonical shared-world state plus per-player data — is live, but every concrete mechanism it names is from the Rust implementation and has been replaced by different Python constructs: `SharedGameSession`/`PlayerState` classes do not exist (replaced by `GameSnapshot.player_seats` dict + `SharedWorldDelta`); `sync_to_locals`/`sync_from_locals` do not exist (replaced by `build_shared_world_delta`/`merge_shared_delta_into_snapshot`); `TargetedMessage` does not exist (replaced by `SessionRoom.broadcast` with per-socket outbound queues); and `resolve_region()` fuzzy co-location does not exist as described (region co-location is now adjacency-list based in `PartyFormationEntry`). The ADR also still shows Rust code samples and a Rust file path (`shared_session.rs`) as the implementation pointer.
- **Evidence:** sidequest-server/sidequest/game/shared_world_delta.py:1-228 (SharedWorldDelta + merge/build functions, live); sidequest-server/sidequest/server/session_room.py:919-958 (broadcast via per-socket outbound queues, not TargetedMessage); sidequest-server/sidequest/server/websocket_session_handler.py:2023-2045 (build_shared_world_delta + handshake in NARRATION_END); sidequest-server/sidequest/server/rest.py:272 (comment: "Phase N: multiplayer SharedGameSession sync" not yet real); sidequest-server/sidequest/game/session.py:892 (player_seats dict field; no PlayerState class)

### ADR-47 · MINOR · Prompt Injection Sanitization Layer
- **Claimed:** `accepted/live`
- **Finding:** The sanitization function exists, is live, and is wired correctly: sanitize_player_text() in sidequest/protocol/sanitize.py is called in player_action.py:287 before any agent or MP buffer write sees the text. However, the ADR claims enforcement happens "at the protocol deserialization layer" (implying Pydantic field validation on PlayerActionPayload), but the Python port calls it explicitly in the handler — PlayerActionPayload has no field_validator invoking it. Additionally, the regex patterns diverge from the ADR spec: the code covers human_turn/ai_turn/user-input but lacks the ADR-listed prompt/instruction tags, and the bracket-markers pattern covers SYSTEM/INST but not the ADR-listed CONTEXT variant.
- **Evidence:** sidequest-server/sidequest/protocol/sanitize.py:82 (sanitize_player_text function exists); sidequest-server/sidequest/handlers/player_action.py:10,287 (imported and called); sidequest-server/sidequest/protocol/messages.py:56-73 (PlayerActionPayload has no field_validator calling sanitize — deserialization boundary claim is false); sidequest-server/sidequest/protocol/__init__.py:115,154 (exported from protocol package)

### ADR-67 · MINOR · Unified Narrator Agent — Collapse Multi-Agent into Single Narrator
- **Claimed:** `accepted/live`
- **Finding:** The core unification decision is live and correct — CreatureSmith, Dialectician, Ensemble, and HaikuClassifier are gone, the narrator handles all intents, and the confrontation_intent_validator inference site (promised in the Post-Spec-2026-05-20 note) is wired. However, both the ADR body and the Post-ADR-098 note describe the narrator as using "fresh claude -p calls," when in reality the claude and ollama backends are retired and raise NarratorBackendRetired at construction (ADR-101/story-61-9); the sole viable narrator is the Anthropic SDK. Additionally, the specific tool names promised in the ADR (resolve_combat, resolve_chase, resolve_dialogue, initiate_combat, initiate_chase) do not exist — the live tools are advance_confrontation, apply_damage, etc.
- **Evidence:** sidequest-server/sidequest/agents/narrator.py:7-8 (ADR-067 confirmed, specialist agents absorbed); sidequest-server/sidequest/agents/llm_factory.py:16,59-66 (_RETIRED_BACKENDS includes "claude", NarratorBackendRetired raised for claude/ollama); sidequest-server/sidequest/agents/orchestrator.py:1712 ("Story 61-9 retired the legacy claude -p / Ollama narrator path"); sidequest-server/sidequest/server/narration_apply.py:3425 (confrontation_intent_validator.validate wired); sidequest-server/sidequest/server/websocket_session_handler.py:2208 (classified_intent populated from applied_outcome); sidequest-server/sidequest/agents/tools/ (no resolve_combat, resolve_chase, resolve_dialogue, initiate_combat, or initiate_chase tools)

### ADR-75 · MINOR · 3D Dice Rendering — Three.js + Rapier Physics Overlay
- **Claimed:** `accepted/partial`
- **Finding:** The ADR's self-amendments (2026-05-02, 2026-05-28, 2026-05-31) accurately describe the two architectural pivots (overlay→inline tray, gesture→auto-roll) and the dice.yaml→hardcoded-TypeScript-map deviation. However, the ADR consistently states DiceScene.tsx and replayThrowParams.ts live at sidequest-ui/src/dice/ — they actually live in the separate sibling package @local/dice-lib (/Users/slabgorb/Projects/dice-lib/src/), which InlineDiceTray imports. Minor line-number stale references in the amendments also exist: ConfrontationOverlay mount is at :662 (ADR says :325) and the App.tsx removal comment is at :54-55 (ADR says :44-45).
- **Evidence:** sidequest-ui/src/dice/InlineDiceTray.tsx:1-11 (no-gesture docstring), :36-139 (GENRE_DICE_THEMES 11 entries), :226 (theme resolution); sidequest-ui/src/components/ConfrontationOverlay.tsx:662 (InlineDiceTray mount — ADR says :325); sidequest-ui/src/App.tsx:54-55 (overlay-removal comment — ADR says :44-45); /Users/slabgorb/Projects/dice-lib/src/DiceScene.tsx (RigidBody/RapierRigidBody wiring — ADR implies sidequest-ui/src/dice/DiceScene.tsx); /Users/slabgorb/Projects/dice-lib/src/replayThrowParams.ts:40-45 (seed-based deterministic replay); sidequest-ui/package.json (three@^0.183.2, @react-three/fiber@^9.5.0, @react-three/rapier@^2.2.0 confirmed); find sidequest-content -name dice.yaml → zero hits

### ADR-077 · MINOR · Dogfight Subsystem via StructuredEncounter Extension
- **Claimed:** `accepted/live`
- **Finding:** The architectural decision is correctly implemented — StructuredEncounter extended with sealed_letter_lookup resolution mode, per_actor_state on EncounterActor, _from: loader pattern, and YAML content files all live in production code. However, the implementation-status addendum's specific file:line pointers are stale (narration_apply.py grew ~4x after the addendum was written, pushing the dogfight branch from the claimed line 1176 to actual line 3585), and four of the seven OTEL spans declared "non-negotiable" in the decision section remain explicitly deferred per the telemetry module's own docstring.
- **Evidence:** Core implementation live: sidequest-server/sidequest/server/dispatch/sealed_letter.py (production handler), sidequest-server/sidequest/server/narration_apply.py:3585 (dispatch branch), sidequest-server/sidequest/genre/models/rules.py:310–328 (ResolutionMode enum with sealed_letter_lookup), sidequest-server/sidequest/game/encounter.py:126 (per_actor_state field), sidequest-content/genre_packs/space_opera/rules.yaml:550–638 (dogfight confrontation + _from: loader), sidequest-content/genre_packs/space_opera/dogfight/ (all five content files present). OTEL gap: sidequest-server/sidequest/telemetry/spans/dogfight.py:6–7 explicitly defers 4 of 7 ADR-mandated spans (gun_solution_fired, energy_depleted, skill_tier_resolved, ace_instinct_used). Line-number pointers in the implementation-status addendum are wrong: ADR claims rules.yaml:324 (dogfight entry) but it is at line 550; ADR claims narration_apply.py:1176 (sealed-letter path) but it is at line 3585; ADR claims orchestrator.py:445–447,1111–1121 for dogfight routing but dogfight content is at lines 749–753 and 1890–1900.

### ADR-081 · MINOR · Advancement Effect Variant Expansion (v1)
- **Claimed:** `accepted/deferred`
- **Finding:** The ADR is correctly marked deferred and the two new variants (AllyEdgeIntercept, ConditionalEffectGating) are confirmed absent from production code. However, the 2026-05-02 implementation status section contains two stale claims: it names the base classes as AdvancementEffectEdgeMaxBonus and AdvancementEffectEdgeRecovery (ADR-114 renamed them to AdvancementEffectHpMaxBonus/AdvancementEffectHpRecovery), and it reports the character YAML drafts moved to genre_workshopping/heavy_metal/worlds/evropi/_drafts/ which no longer exists — those files are at genre_packs/heavy_metal/worlds/evropi/_drafts/character-progression/ (the genre_workshopping staging tree was retired 2026-06-03).
- **Evidence:** sidequest-server/sidequest/genre/models/advancement.py:69-79 (classes renamed to AdvancementEffectHpMaxBonus / AdvancementEffectHpRecovery by ADR-114); sidequest-content/genre_packs/heavy_metal/worlds/evropi/_drafts/character-progression/ (files exist here, not at the genre_workshopping/ path named in the implementation status section); genre_workshopping/ directory does not exist (retired 2026-06-03 per CLAUDE.md)

### ADR-92 · MINOR · Scene Harness — Dev-Gated HTTP Endpoint for Scenario Fixtures
- **Claimed:** `accepted/partial`
- **Finding:** The 2026-05-28 amendment correctly describes the server as fully implemented (route, hydrator, OTEL, tests all live and wired at app.py:307-314), but three stale-info items remain: (1) the frontmatter `implementation-status: partial` was never updated to `complete`, so tooling that reads frontmatter still reports partial; (2) the Decision body and amendment both cite `App.tsx:1183–1217` for the UI harness code, but the actual lines are 1616–1650; (3) the Decision item 1 still describes `DEV_SCENES=1` gating as the design, while the amendment notes this was removed in favor of Cloudflare Zero Trust at the tunnel layer — the Decision section was not corrected in-place.
- **Evidence:** sidequest-server/sidequest/server/app.py:304-314 (router unconditionally mounted, no DEV_SCENES gate); sidequest-server/sidequest/server/scene_harness_router.py:52-176 (route + OTEL spans live); sidequest-server/sidequest/game/scene_harness.py:1-679 (hydrator live, docstring line 4 still says SqliteStore but implementation uses _build_pg_repos_for_slug); sidequest-ui/src/App.tsx:1616-1650 (UI at lines 1616-1650, not 1183-1217 as ADR states throughout); docs/adr/092-scene-harness-http-endpoint.md:11 (frontmatter implementation-status still "partial" despite 2026-05-28 amendment declaring it complete); scenarios/fixtures/ contains 12 YAMLs, not the 4 the ADR describes

### ADR-93 · MINOR · Confrontation Difficulty Calibration v1
- **Claimed:** `accepted/live`
- **Finding:** The core calibration is live and correct: the ±1 tie band is implemented in opposed_check.py (effective range [-1,+1] because _SUCCESS_SHIFT=2 and _FAIL_SHIFT=-2 leave only -1/0/+1 as Tie), and the original four-pack target packs that use opposed_check (caverns_and_claudes, mutant_wasteland) are correctly calibrated to stats=10 and threshold=7. However, the 2026-05-04 amendment's claim that space_opera's ship_combat "uses resolution_mode: opposed_check" is stale: ship_combat uses beat_selection with win_condition: hp_depletion in the current YAML. Additionally, two packs added after the calibration sprint (heavy_metal and spaghetti_western) have opposed_check combat with opponent_default_stats=12 and threshold=10, both uncalibrated against the ADR-093 spec.
- **Evidence:** sidequest-server/sidequest/game/opposed_check.py:55-58 (tie band constants _SUCCESS_SHIFT=2/_FAIL_SHIFT=-2 → effective Tie range -1..+1, matching ADR); sidequest-content/genre_packs/space_opera/rules.yaml:229 (ship_combat uses resolution_mode: beat_selection, NOT opposed_check as the ADR amendment claims); sidequest-content/genre_packs/heavy_metal/rules.yaml:185-197 (opposed_check combat with opponent_default_stats 12 and threshold 10 — uncalibrated post-ADR pack); sidequest-content/genre_packs/spaghetti_western/rules.yaml:329-344 (opposed_check combat with stats 12 and threshold 10 — uncalibrated); sidequest-content/genre_packs/mutant_wasteland/rules.yaml:96-112 (correctly calibrated: stats 10, threshold 7); sidequest-content/genre_packs/caverns_and_claudes/rules.yaml:80-93 (correctly calibrated: stats 10, threshold 7); sidequest-server/sidequest/telemetry/spans/encounter.py:197-202 (encounter.opposed_roll_resolved span exists)

### ADR-104 · MINOR · Perception Filtering at the Tool Layer
- **Claimed:** `accepted/partial`
- **Finding:** The core two-layer architecture (NarratorPerceptionFilter at the tool layer + PerceptionRewriter in the MP fan-out) is correctly described and both are live in production. However, the ADR names emitters.py:283 as the PerceptionRewriter call site; the actual calls are at lines 567 and 607 due to file growth. Additionally, the per-tool rule table in the ADR implies all 10 named tools have rules in _RULES, but only query_character and query_npc are registered; the other 8 tools use handler-side filtering or explicitly defer registration, which is consistent with partial status but mismatched against the rule table framing.
- **Evidence:** sidequest-server/sidequest/server/emitters.py:18 (import), :567 and :607 (actual call sites for rewrite_for_recipient); ADR claims emitters.py:283. sidequest-server/sidequest/agents/narrator_perception_filter.py:106,129 (only 2 of the 10 claimed tool rules registered via register_rule: query_character and query_npc). sidequest-server/sidequest/agents/orchestrator.py:3925 (NarratorPerceptionFilter wired in production). sidequest-server/sidequest/agents/tools/query_lore.py:3 "no perception rule (yet)"; query_scene_state.py:3 "no perception rule"; lookup_monster.py:3 "no perception rule"; query_gossip.py:3 "no perception rule (yet)"; query_scenario_clues.py:3 "no perception rule".

### ADR-115 · MINOR · Persistence Substrate Migration — SQLite-Per-Session to PostgreSQL
- **Claimed:** `accepted/live`
- **Finding:** The migration is fully live and correct in all load-bearing respects — Postgres pool, row locks, Alembic DDL, repository protocols, startup fail-loud check, and importer — but the ADR's decision text specifies "anyio.to_thread" as the mechanism for offloading sync DB calls from async handlers. anyio is not a declared dependency and no such offloading exists; sync psycopg3 methods are called directly inside async WebSocket handler code, blocking the event loop. The legacy forensic_query.py with _ro_connect also survives on disk but is not imported by any production code path (REST layer uses PgForensicReader exclusively).
- **Evidence:** ADR decision text: "Calls from async handlers offload via anyio.to_thread." — anyio is absent from pyproject.toml dependencies; sidequest-server/pyproject.toml:18-19 shows only psycopg[binary] and psycopg-pool; grep for anyio/to_thread across all production sidequest/ code yields zero hits except ollama_client.py (unrelated). Production async handlers call sync PgSaveRepository methods directly: websocket_session_handler.py:480 calls sd.repository.save(...) and :1195 calls sd.repository.save(snapshot) inline, no thread offloading. All other claimed mechanisms verified live: PgSaveRepository at sidequest/game/pg/save_repository.py:45, PgDungeonRepository at pg/dungeon.py:308, PgTelemetrySink at pg/telemetry.py:45, PgForensicReader at pg/forensic.py:310; per-session row lock at pg/_conn.py:21 (SELECT ... FOR UPDATE); SIDEQUEST_DATABASE_URL/MissingDatabaseUrlError at game/db_config.py:13,18; pool.wait(timeout=10.0) at server/app.py:250; sessions table with integer session_id PK + session_slug UNIQUE at alembic/versions/0001_initial_unified_schema.py:31-32; events PK (session_id, seq) at 0001:117; Alembic versions 0001 and 0002 present; SqliteStore/SAVE_WRITE_LOCK classes absent from all production code; importer.py:59 import_sqlite_save exists exactly as described.

### ADR-116 · MINOR · A Confrontation Requires an Other — Participant Membership Invariant, Single Opponent-Seater, End-on-No-Other
- **Claimed:** `accepted/partial`
- **Finding:** The architectural decisions in §1-§4 are all correctly implemented — NoOpponentAvailableError, the room-scan seater, _ADVERSARIAL_CATEGORIES frozenset({"combat","movement"}), participant.joined spans, _resolve_if_no_opponent_remains, and the lifecycle detector wiring in websocket_session_handler.py are all live in production code. However, the 2026-05-28 amendment contains a comprehensive set of specific file:line references that are systematically wrong across all cited locations (off by 12-1400+ lines each), making it unreliable as a navigation aid and indicating it was not updated after subsequent code growth. The partial status is accurate: §3 social/pre_combat guard generalization and §2 bestiary sourcing remain deferred exactly as the ADR scoped.
- **Evidence:** encounter_lifecycle.py:50 (NoOpponentAvailableError, ADR claims :38); encounter_lifecycle.py:940 (raise, ADR claims :591); encounter_lifecycle.py:386/_ADVERSARIAL_CATEGORIES (ADR claims :330); encounter_lifecycle.py:409/_is_adversarial (ADR claims :344); encounter_lifecycle.py:519/default_side (ADR claims :431); encounter_lifecycle.py:1041/participant_joined_span (ADR claims :686-692); telemetry/spans/encounter.py:105/SPAN_PARTICIPANT_JOINED (ADR claims :82); narration_apply.py:4375 call + 4564 def for _resolve_if_no_opponent_remains (ADR claims :3155); confrontation.py:166/except NoOpponentAvailableError (ADR claims :134); websocket_session_handler.py:1765-1766/build_lifecycle_snapshot import (ADR claims :1735-1737); encounter_lifecycle.py:922/social exempt comment (ADR claims :577)

### ADR-005 · TRIVIAL · Background-First Pipeline
- **Claimed:** `accepted/live`
- **Finding:** The ADR's three concrete implementation pointers all cite wrong line numbers: the embedding worker spawn is at lore_embed.py:192 (not 154), the writer task at websocket.py:99 (not 65), and the render background spawn at websocket_session_handler.py:2954 (not 3274). The underlying architectural decision — narrator on the critical path, image/audio/embedding spawned as background asyncio.create_task calls — is correctly implemented and live.
- **Evidence:** ADR claims lore_embed.py:154, websocket.py:65, websocket_session_handler.py:3274 — actual asyncio.create_task calls are at lore_embed.py:192, websocket.py:99, websocket_session_handler.py:2954. Line 3274 of websocket_session_handler.py is inside a watcher-publish dict literal, not a task spawn. The core decision (background tasks via asyncio.create_task for render/embed, narrator on critical path) is correctly implemented.

### ADR-15 · TRIVIAL · Character Builder State Machine
- **Claimed:** `accepted/live`
- **Finding:** The ADR claims five states (IDLE → IN_PROGRESS → AWAITING_FOLLOWUP → CONFIRMATION → COMPLETE) but the live Python implementation intentionally implements only three (InProgress, AwaitingFollowup, Confirmation); the module docstring explicitly states "No IDLE or COMPLETE states — construction and consumption are the boundaries." The core decision (state machine driven by scenes in char_creation.yaml, producing NarrativeHook objects) is fully live and wired into production handlers. The Rust struct illustration for NarrativeHook is historical per the Rust-port rule and is not stale.
- **Evidence:** sidequest-server/sidequest/game/builder.py:4 (module docstring: "No IDLE or COMPLETE states; construction and consumption are the boundaries"); builder.py:455-475 (only three concrete BuilderPhase subclasses: InProgress, AwaitingFollowup, Confirmation — no IDLE or COMPLETE); builder.py:1 (class CharacterBuilder, fully implemented); handlers/character_creation.py:10, server/websocket_handlers/chargen_mixin.py:29 (production wiring); sidequest-content/genre_packs/space_opera/char_creation.yaml:1 (scene-driven YAML format matching ADR schema)

### ADR-63 · TRIVIAL · Dispatch Handler Splitting — By Pipeline Stage
- **Claimed:** `accepted/live`
- **Finding:** The core decision — split dispatch into pipeline-stage submodules under sidequest-server/sidequest/server/dispatch/ — is fully live and wired more extensively than described (24 modules vs. 8 named). The only stale item is in the post-port mapping: the ADR names `opening_hook.py` as the session-opener module, but the actual file is `opening.py`; the file's own docstring states "Replaces the old opening_hook.py," confirming the rename happened after the ADR was written.
- **Evidence:** sidequest-server/sidequest/server/dispatch/ (directory exists, 24 modules confirmed); dispatch/opening.py:1-5 (docstring says "Replaces the old opening_hook.py"); chargen_loadout.py, chargen_summary.py, combat_brackets.py, confrontation.py, culture_context.py, encounter_lifecycle.py, scenario_bind.py all confirmed present; websocket_session_handler.py:84 and websocket_handlers/chargen_mixin.py:71-74 import from the dispatch package in production code

### ADR-079 · TRIVIAL · Genre Theme System Unification
- **Claimed:** `accepted/live`
- **Finding:** The core decision is fully implemented: ThemeProvider deleted, JS bridge removed, useGenreTheme is the sole consumer and sets data-genre on every render, all 11 genre pack CSS files use :root[data-genre]. The one stale-info point is the ADR body's specificity notation: §Specificity Solution states :root[data-genre] is "(0,1,1)" but the actual CSS specificity is (0,2,0) — :root is a pseudo-class (0,1,0) plus [data-genre] is an attribute selector (0,1,0) — and the code's own comment at useGenreTheme.ts:166 correctly states "(0,2,0)". The original §Changes Required also listed "Keep: Google Font loading" but the 2026-05-31 amendment documents that genre @font-face rules now deliver fonts from R2 (cdn.slabgorb.com), eliminating the Google Fonts CDN path, which is a minor content drift from that one line.
- **Evidence:** sidequest-ui/src/hooks/useGenreTheme.ts:166 (code comment: "(0,2,0)"); docs/adr/079-genre-theme-unification.md:40 (ADR claims "(0,1,1)"); useGenreTheme.ts:175 (data-genre set); sidequest-content/genre_packs/*/client_theme.css:1 (all 11 use :root[data-genre]); no ThemeProvider.tsx exists under sidequest-ui/src/providers/; no ThemeProvider/ThemeContext in any src/ file; App.tsx:540 calls useGenreTheme(messages, connected)

### ADR-090 · TRIVIAL · OTEL Dashboard Restoration after Python Port
- **Claimed:** `accepted/live`
- **Finding:** All core mechanisms described in the ADR and its amendment are live and correctly wired: Validator with five checks, asyncio.Queue(maxsize=32) drop-oldest, heartbeat task, PhaseTimings NULL sentinel, SPAN_ROUTES routing table, WatcherSpanProcessor, /ws/watcher endpoint, and ConsoleSpanExporter gating. The single stale-info: the ADR says SPAN_ROUTES is "colocated with span constants in spans.py" (implying a flat module), but the implementation evolved into a spans/ package with _core.py holding SPAN_ROUTES and per-domain submodules registering into it — the mechanism is identical, only the file path changed.
- **Evidence:** sidequest-server/sidequest/telemetry/validator.py:345 (Validator class), :349 (asyncio.Queue(maxsize=32)), :358-362 (five check registrations), :401-403 (task creation), :441 (_validate), :529-546 (check loop); sidequest-server/sidequest/telemetry/phase_timing.py:22 (PhaseTimings), :90 (_NullPhaseTimings), :99-104 (__init__ no super()), :128 (PhaseTimings.NULL); sidequest-server/sidequest/telemetry/turn_record.py:39-41 (snapshot_before_hash + snapshot_after + delta); sidequest-server/sidequest/server/app.py:147,150-152 (Validator wired at startup), :293 (/ws/watcher endpoint); sidequest-server/sidequest/server/watcher.py:50 (WatcherSpanProcessor), :100-104 (SPAN_ROUTES routing lookup); sidequest-server/sidequest/telemetry/spans/_core.py:44 (SPAN_ROUTES dict); sidequest-server/sidequest/telemetry/setup.py:45-46 (SIDEQUEST_OTEL_CONSOLE gate)

### ADR-95 · TRIVIAL · Daemon Music Tier via ACE-Step
- **Claimed:** `accepted/live`
- **Finding:** The entire music generation pipeline (ACE-Step adapter, MusicPipeline, r2_writer.upload_pack_asset, pipeline_factory, daemon dispatch, watcher events, generate_music.py script) is fully implemented and wired exactly as the ADR describes. The single stale fact is the codec bitrate: the ADR states 96 kbps (line 63) but the production code uses 160 kbps; the code's own docstring records the bump and explains the rationale, confirming this is a deliberate post-ADR change that was never reflected in the ADR. CLAUDE.md (daemon) line 151 also still says "libopus 96k", compounding the stale reference.
- **Evidence:** sidequest-daemon/sidequest_daemon/media/music_pipeline.py:27-44 — _run_ffmpeg uses `-b:a 160k`, not 96k; the docstring explicitly says "The original 96k was an undocumented first-write default... bumped per durable-retention". ADR claims "ffmpeg -c:a libopus -b:a 96k" (ADR line 63). All other named modules exist and are wired: music_pipeline.py (MusicPipeline.derive_r2_key, generate), ace_step_adapter.py (AceStepAdapter, InferenceResult, prepare_inference_params), r2_writer.py (upload_pack_asset), pipeline_factory.py (MediaPipelineFactory.init_music), daemon.py:205 (MUSIC_TIERS), daemon.py:208 (dispatch_request), daemon.py:513 (_handle_client music short-circuit), telemetry/watcher_bridge.py (emit_watcher_event), scripts/generate_music.py (discover_jobs, is_in_r2, filter_jobs_by_track, send_render). audio/ mixer module source deleted (only __pycache__ remains). ml/memory_manager.py absent.

### ADR-098 · TRIVIAL · Stateless Narrator Turns — Drop --resume, Bounded Per-Turn Prompts
- **Claimed:** `accepted/live`
- **Finding:** The decision is fully implemented: no --resume, no NarratorPromptTier/select_prompt_tier, no tier gating, send_stateless called, PROMPT_BUDGET_BYTES_HARD hard-refuse at _check_oversized_prompt, retry-once policy, degraded "world holds its breath" return, and bounded:True on prompt_assembled. Two stale pointers exist: the ADR names `_check_oversized_prompt()` at `orchestrator.py:2966` but the function is defined at line 3316 (line 2966 is a variable assignment); the Orchestrator class docstring at line 1513 still says "Session management follows ADR-066 (persistent Opus sessions via --resume)" despite ADR-098 superseding ADR-066.
- **Evidence:** orchestrator.py:238 (PROMPT_BUDGET_BYTES_HARD=2_000_000), :2965 (ADR-098 stateless comment), :3316 (_check_oversized_prompt def — ADR claims line 2966), :3264 (_invoke_with_retry_once docstring), :2840 (bounded:True in prompt_assembled event), :1513 (stale class docstring still says "ADR-066 persistent Opus sessions via --resume")

## Split-brain — conflicting current realities (0)

_None confirmed._
