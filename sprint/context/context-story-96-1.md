# Story 96-1 Context

## Title
Rewrite content-coupled server tests against fixtures (decouple from live packs)

## Metadata
- **Story ID:** 96-1
- **Type:** refactor
- **Points:** 8
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** sidequest-server
- **Epic:** Genre/World Boundary Follow-ups — fixture decoupling & tier-leak fixes

## Problem
Follow-up from epic 94 (genre=rulebook / world owns cast+catalog, ADR-140). The
content repoint exposed server tests wrongly coupled to LIVE content packs —
content-only changes must never turn server tests red. Validators validate
content; tests test fixtures. PR sidequest-server#726 skipped the
content-coupled tests as a stopgap (skip reasons cite "story 94-4"; the
fixture-rewrite story now lives here as 96-1).

## Technical Approach
_High-level only — TEA/Dev own the details._
- Inventory: the tests skipped by sidequest-server#726 (grep skip reasons
  mentioning 94-4) are the work list.
- Rewrite each against synthetic fixtures instead of live packs from
  `SIDEQUEST_GENRE_PACKS`.
- Project rule (memory: tests must not point at live content): fixtures
  referencing a real world/pack slug are the anti-pattern being removed —
  don't preserve the coupling under a new name.
- Note the sibling story 96-2 owns the Earthman tier-leak guard test
  (`test_earthman_boon_is_world_tier_not_engine_hardcode`, deliberately RED) —
  out of scope here.

## Scope
- In scope: the server tests skipped by #726 as content-coupled; any fixture
  infrastructure needed to rewrite them.
- Out of scope: the Earthman race-boon tier leak (story 96-2); content repo
  changes; validator changes.

## Acceptance Criteria
- All tests skipped by sidequest-server#726 as content-coupled are either
  rewritten against fixtures or explicitly retired with rationale — no skips
  citing 94-4/96-1 remain.
- No server test depends on a live pack/world slug from sidequest-content.
- Full server suite (with `SIDEQUEST_DATABASE_URL` and `SIDEQUEST_GENRE_PACKS`
  set) is no worse than the recorded baseline.

---
_Generated by `pf context create story 96-1` from the sprint YAML._
