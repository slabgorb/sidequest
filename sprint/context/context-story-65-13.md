# Story 65-13 Context

## Title
65-9 review follow-ups: Cast docstring accuracy, span-test rigor, chrome-contract guard + AC8 reference_portrait spans

## Metadata
- **Story ID:** 65-13
- **Type:** refactor
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Content Infrastructure — R2 asset tracking and audit

## Problem
Non-blocking follow-ups from the 65-9 review (Reviewer APPROVED). All 10 findings + the full audit trail are archived in sprint/archive/65-9-session.md (Delivery Findings + Design Deviations → Architect reconcile). Server-only; reference-page Cast section. Pattern: epic 76 ← 75-6.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- DOC: Fix misleading OTEL span semantics — _cast_portrait_img_html reuses scrapbook.npc_portrait_{resolved,not_found} on the reference page, but those spans' docstrings (scrapbook.py:133,152) describe scene-time scrapbook-ref attachment. Either note the dual-use, or implement AC8 (below) which supersedes it.
- AC8 (deferred from 65-9): Add dedicated reference_portrait_resolved/reference_portrait_not_found spans for the reference Cast gate (distinguish 'authored but not on R2' from 'not authored'); migrate test_cast_portrait_decisions_emit_spans to them. Resolves the DOC span-semantics finding.
- DOC: Fix the lying load_cast_entries docstring (reference_renderer.py:1218) — the genre loader's _load_portrait_manifest does NOT drop non-dicts (it model_validates and would raise); only the two-shape tolerance is shared.
- TEST: Strengthen test_cast_portrait_decisions_emit_spans with complement assertions (_ABSENT_SLUG not in resolved; _PRESENT_SLUG not in not_found) so the span test alone distinguishes a correct gate from an always-resolve gate.
- TEST: Add match=r"missing 'key'" to test_load_manifest_entry_missing_key_raises_loudly; add load_r2_manifest_keys.cache_clear() isolation; assert the AC2 500 is manifest-specific; add a graceful no-portrait_manifest.yaml world test (load_cast_entries returns [] → 200, no cast section).
- RULE #3: Annotate test fixture params gated_client: TestClient, otel_capture at test_reference_cast_manifest_gate.py:200,225 (pre-existing pattern in the POI suite too).
- EDGE: Guard load_cast_entries against a non-list 'characters:' value (e.g. characters: 42) with isinstance(chars, list) — currently raises an uncaught TypeError → unclean (but still loud) 500.
- CHROME: Resolve the ref-card__portrait undefined CSS class (allowlist with justification or add a CSS rule) AND extend the chrome-wiring fixture (test_reference_chrome_wiring.py:_seed_space_opera_world) to render a cast+POI-bearing world so the lore-page image classes (ref-card__poi, ref-card__portrait) are actually validated — closes a pre-existing blind spot.
- REFACTOR (optional): unify the POI + Cast manifest gates so a both-features world emits one reference_manifest_loaded span per render (load once, pass keys to both gates) instead of one-per-gate.

---
_Generated by `pf context create story 65-13` from the sprint YAML._
