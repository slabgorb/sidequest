# Story 82-11 Context

## Title
Parse SIDEQUEST_NARRATOR_ITERATION_CAP once at startup, not per-turn (82-9 follow-up; narrator.py:86 + orchestrator.py:4125)

## Metadata
- **Story ID:** 82-11
- **Type:** chore
- **Points:** 1
- **Priority:** p3
- **Workflow:** trivial
- **Repo:** sidequest-server
- **Epic:** Surface the Dark Subsystems — Wire Consumers + OTEL for Overstated ADRs

## Problem
82-9 non-blocking carry-forward. resolve_narrator_iteration_cap() (sidequest/agents/narrator.py:86) reads and parses os.environ['SIDEQUEST_NARRATOR_ITERATION_CAP'] on EVERY turn (called from orchestrator.py:~4125). The toggle is an operator/startup config, not a per-turn input. Parse it once at startup (module-load or app-init) and reuse, OR — if per-turn re-read is intentional for live-tuning — document that intent inline. Either resolution is acceptable; the point is to stop the silent per-turn os.environ parse+validate without an explicit reason. Fail-loud validation on a bad value (current ValueError behavior) must be preserved.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- SIDEQUEST_NARRATOR_ITERATION_CAP is parsed/validated once (startup or first-use memoization), not on every narrator turn — OR a one-line inline comment documents why per-turn re-read is intentional.
- Bad-value fail-loud behavior is preserved (a non-positive-integer value still raises, per No Silent Fallbacks); a test covers both the valid-cap and invalid-cap paths.

---
_Generated by `pf context create story 82-11` from the sprint YAML._
