id: '77'
type: epic
title: Quest & Stakes Substrate
description: 'Implementation of ADR-137 (quest & stakes substrate). Design story 77-1 is DONE — ADR-137 decides Option C (seed-at-creation + typed record_quest/set_stakes tools) as engine core with the UI panel as a committed fast-follow, promotes quest_anchors to first-class (live consumer in orbital/course.py:157), and consolidates to one mechanism (retire the legacy quest_updates lane + the apply_world_patch quest/stakes escape-hatch paths). Root cause: a lane exists to UPDATE a quest but none to CREATE/anchor one; stakes only flow from a trope handshake that never fires in prose packs. See docs/adr/137-quest-stakes-substrate.md §Implementation Stories. Reviewer (The Merovingian) carried 5 non-blocking findings into these stories.'
priority: p2
status: in_progress
repos: server,ui
stories:
  - id: 77-1
    title: Seed-at-creation — quest_anchor + quest_log + active_stakes from PC drive/calling
    points: 3
    priority: p2
    status: done
    started: '2026-06-02'
    repos: server
    workflow: tdd
    acceptance_criteria:
      - "Verify behavior against a no-drive-scene prose pack (e.g. wry_whimsy): Character.drive defaults to '' when the genre has no drive-shaped scene (builder.py:423), so seed-at-creation must NOT be assumed to close the gap there — Option B (77-2) is the load-bearing fix for prose packs"
      - 'Empty-drive path degrades LOUDLY: emit quest.seeded_at_creation span carrying a WARNING-severity attribute on empty seed (No Silent Fallbacks), never a silent no-op'
    completed: '2026-06-02'
    review_findings: 'HIGH: seed_quest_spine unconditionally overwrites world-authored active_stakes (flickering_reach/annees_folles opening chapters); fix = fill-not-clobber guard + preserving test'
    review_verdict: approved
  - id: 77-2
    title: Typed quest/stakes narrator tools — record_quest + set_stakes (ADR-102)
    points: 5
    priority: p2
    status: done
    started: '2026-06-03'
    repos: server
    workflow: tdd
    acceptance_criteria:
      - 'Bound narrator-controlled state: reuse the existing _ACTIVE_STAKES_GUARDRAIL=1024 (narration_apply.py:5762) for set_stakes, and add a cardinality cap for quest_log / quest_anchors at the ADR-102 / Pydantic schema layer (unbounded narrator strings persist to Postgres → state-bloat vector)'
    completed: '2026-06-03'
    branch: feat/77-2-typed-quest-stakes-tools
    review_verdict: approved
  - id: 77-3
    title: Promote quest_anchors to first-class WorldStatePatch field + wire to orbital course
    points: 3
    priority: p2
    status: done
    started: '2026-06-04'
    repos: server
    workflow: tdd
    completed: '2026-06-04'
  - id: 77-4
    title: One-mechanism cleanup — retire quest_updates lane + strip apply_world_patch quest/stakes paths
    points: 3
    priority: p3
    status: done
    started: '2026-06-04'
    repos: server
    workflow: tdd
    completed: '2026-06-04'
    type: refactor
  - id: 77-5
    title: Quest/objective panel — render quest_log + quest_anchors + active_stakes (quests payload)
    description: 'Server quests projection landed (77-8, 2026-06-04). This render-only UI panel mirrors the rich QuestsPayload (quest_log + quest_anchors + active_stakes) into a TS type and renders it as a dockable GameBoard widget, mirroring the ADR-136 RelationshipsPanel pattern. No game-logic writes, no fabrication of payload shape — consume the full server projection shape.'
    points: 5
    priority: p3
    status: done
    assigned_to: dev
    started: '2026-06-04'
    repos: ui
    workflow: tdd
    completed: '2026-06-04'
    branch: feat/77-5-quest-objective-panel
    review_findings: 'REJECT (3 blocking): (1) No-Silent-Fallbacks violation — dangling-reference quest anchors silently dropped in QuestsPanel orphan filter, with a comment that falsely claims otherwise; (2) isEmptySpine throws on malformed/missing-field payload instead of degrading to empty-state — less robust than the mirrored RelationshipsPanel, contra AC2; (3) orphan-anchor render branch has zero test coverage. Fixes are cheap+testable. Route to TEA for red rework.'
    review_verdict: approved
  - id: 77-6
    title: Author wry_whimsy seed_tropes deck (ADR-128) — active_seeds carve-out
    points: 2
    priority: p3
    status: backlog
    repos: content
    workflow: trivial
  - id: 77-7
    title: Engine lull-escalation — drive a Bang/complication when turns_since_meaningful climbs (ADR-024/025/128)
    description: |-
      GAP (Keith playtest, wry_whimsy/gulliver): the engine is PASSIVE — 'had to prod a bit to get the action going.' Both set-pieces were player-initiated; the narrator reacted well but never DROVE. Per Living World + Cut-the-Dull-Bits, when a game lulls the engine should PUSH: name a stake, fire a complication, an NPC acts on its own goal. Today nothing fires — the scenario subsystem went silent the whole session (coverage_gap turn 29: subsystem=scenario, silent_turns=10), active_seeds=[], active_stakes=''. For a forever-GM who wants to be SURPRISED, this is the difference between 'fun but I had to prod' and 'fun and it kept coming at me.'

      REUSE MAP (Explore 2026-06-04, develop): the lull SIGNAL already exists — TensionTracker (game/tension_tracker.py) computes boring_streak + drama_weight every turn (fed via observe() at websocket_session_handler.py:~2048) and pacing_hint() ALREADY trips escalation_beat at boring_streak>=escalation_streak (tension_tracker.py:~359-383) — but only as a GENERIC narrator text hint ('The environment shifts — introduce a new element'), consumed by NO engine. The Bang CATALOG already exists — seed_tropes deck (ADR-128): game/seed_deck.py (deterministic draw-without-replacement via SHA-256 of session_id), game/seed_tick.py (ensure_initial_draw/draw_engaged_seed/tick_seeds), rendered to the narrator by agents/seed_context_builder.py; SeedTrope.narrative_hint (genre/models/tropes.py:81) exists. Governor caps live (game/trope_tuning.py: MAX_SIMULTANEOUS_ACTIVE, FIRE_COOLDOWN_TURNS). SPAN_SEED_FIRED exists (telemetry/spans/seed.py) but has NO consumer. MISSING: a SELECTOR that, on the live lull signal, picks one active seed (or draws one) and FIRES it as a CONCRETE escalation directive into the next turn — plus an OTEL span proving the engine pushed.

      DESIGN (Architect / White Queen — v1 load-bearing slice, reuse-first, NO new ADR — enacts ADR-024/025/128): (1) TRIGGER reuses the EXISTING signal — boring_streak>=escalation_streak (pacing_hint.escalation_beat present). NO new ADR-025 per-category quiet-turn detector in v1; ride the live ADR-024 tension track. (2) SELECT one seed from snapshot.active_seeds (prefer scene-fit via flavor_tags/delivery_hints; deterministic resume-safe tie-break from (session_id, turn, drawn_ids) — reuse the seed_deck SHA-256 pattern, NO Math.random/wallclock). If active_seeds empty, draw one first (reuse draw_engaged_seed). (3) FIRE — convert the selected seed's narrative_hint into the concrete escalation directive injected for the NEXT turn's narrator context, REPLACING the generic escalation_beat text; mark the seed fired (give SPAN_SEED_FIRED its first real consumer) and advance lifespan via the existing tick. (4) GOVERN — respect FIRE_COOLDOWN_TURNS (never fire two turns running). (5) SEAM — post-bump, peer to tick_seeds (websocket_session_handler.py:~1170-1191), reads sd.tension_tracker, mutates snapshot.active_seeds in place, same shape as tick_tropes/tick_seeds. (6) OTEL is LOAD-BEARING here (the whole point — the lie detector for 'did the engine push or did Claude improvise?'): new routed SPAN_LULL_ESCALATION.

      DEFER (explicit non-goals, documented not built): (a) NPC-autonomous-goal escalation (ADR-053 living-world/gossip is DORMANT; npc_agency is reactive-only) — v2; (b) the full ADR-025 per-category quiet-turn detector (boring_streak is the sufficient v1 lull signal); (c) a separate Bang-catalog struct distinct from seed_tropes. v1 = wire the EXISTING lull signal to fire an EXISTING seed, with proof. If the selector policy (which seed / cooldown composition with the ADR-128 governor) needs ratification during the dev's first design pass, escalate to a thin ADR then (epic-59 pattern). Sibling content: 77-6 (author the wry_whimsy seed_tropes deck — gives the selector something to throw; FIXER already PR'd #347).
    points: 5
    priority: p2
    status: backlog
    repos: sidequest-server
    workflow: tdd
    acceptance_criteria:
      - 'Lull trigger reuses the LIVE signal: when TensionTracker reports boring_streak >= escalation_streak (pacing_hint.escalation_beat present), the new post-bump escalation step engages; below threshold it is a no-op. NO new pacing detector — ride the existing ADR-024 tension track. Test: synth a snapshot with boring_streak at threshold → step fires; below → no-op.'
      - "Select-and-fire an active seed: on lull, select one seed from snapshot.active_seeds (deterministic, resume-safe tie-break from session_id+turn+drawn_ids) and FIRE it — its narrative_hint becomes the concrete escalation directive in the NEXT turn's narrator context, REPLACING the generic 'environment shifts' escalation_beat text. If active_seeds is empty, draw one first (reuse draw_engaged_seed) then fire. Test: active seed present → that seed's hint is the injected directive; empty → a seed is drawn then fired."
      - "Respect the ADR-128 governor: never fire on consecutive turns (FIRE_COOLDOWN_TURNS, game/trope_tuning.py); mark the fired seed's state so it expires normally via tick_seeds. Test: two lull turns in a row → fires turn 1, cooldown-skips turn 2 with reason=cooldown."
      - 'OTEL (LOAD-BEARING — the lie detector for engine-push vs narrator-improv): emit a ROUTED SPAN_LULL_ESCALATION every time the step runs, carrying boring_streak, drama_weight, fired(bool), selected_seed_id|null, reason(fired|cooldown|none_available). It must light the pacing/tension subsystem on the GM panel ③ grid for the firing turn. SPAN_SEED_FIRED gains its first real consumer on fire. Test: span asserted on fire AND on cooldown-skip with correct reason (OTEL span assertion, not source-grep).'
      - "Seam + resume safety + wiring: the step runs post-record_interaction, peer to tick_seeds (websocket_session_handler.py:~1170-1191), reads sd.tension_tracker, mutates snapshot.active_seeds in place; selection is deterministic (no Math.random/wallclock) so a resume re-fires identically. Wiring test (behavioral, not source-grep): drive a real lull turn through the handler and assert the NEXT turn's narrator context carries the seed-derived escalation directive."
      - 'DEFER — explicit non-goals, documented in the story, NOT built: (a) NPC-autonomous-goal escalation (ADR-053 dormant; npc_agency reactive-only); (b) full ADR-025 per-category quiet-turn detector; (c) a separate Bang-catalog struct distinct from seed_tropes. v1 fires an existing seed on the existing lull signal with OTEL proof.'
  - id: 77-8
    title: Project the quests spine to the client — emit quest_log + quest_anchors + active_stakes outbound (RELATIONSHIPS-snapshot analog)
    description: Blocking prerequisite for 77-5 (UI quest/objective panel), surfaced by TEA during 77-5 RED (2026-06-04). ADR-137 server lane 77-1..77-4 all landed (seed, record_quest/set_stakes tools, quest_anchors→WorldStatePatch, legacy-lane cleanup) and STORE the spine — but NONE projects it OUTBOUND to the client. quest_log (rich QuestEntry), quest_anchors, active_stakes live in game state (game/session.py:790,861,867) yet _shared_world_delta_to_state_delta (server/session_state.py:63-99) projects only location/encounter_id/party_formation/magic_state. The legacy StateDelta.quests dict[str,str] is never populated (test_wire_parity.py:75-78 asserts omission). This story ships the missing projection so the UI panel (77-5) has something to render. Mirror the ADR-136 RELATIONSHIPS snapshot pattern (server/websocket_handlers/relationships_emit.py + RelationshipsMessage/RelationshipsPayload) rather than piggybacking the thin legacy quests field. Honors ADR-137 'server lands + OTEL-verified before the panel consumes' sequencing.
    points: 5
    priority: p2
    status: done
    started: '2026-06-04'
    repos: server
    workflow: tdd
    acceptance_criteria:
      - 'AC1 — Rich quests projection on the wire: the client receives quest_log (title + objective + status + anchor_id per quest), quest_anchors (anchor id + owning quest + beat/location resolution), and active_stakes (string) together, in a typed payload analogous to RelationshipsPayload. Test: serialize a populated spine and assert all three fields appear with correct shape on the wire (wire-parity test, like test_wire_parity.py).'
      - 'AC2 — Reactive emit on change: the projection re-broadcasts when seed-at-creation, record_quest, or set_stakes mutate the spine (mirror how relationships_emit fires). Test: mutate via each path → projection carries the new value.'
      - 'AC3 — Empty/seeded states are well-formed: an unpopulated spine projects a clean empty payload (not a white-screen-inducing null soup); a single creation-seeded quest+anchor+stakes projects exactly that. Test: empty snapshot → empty-but-valid payload; seeded snapshot → one entry.'
      - 'AC4 — Wiring: the projection is reachable from the live NARRATION_END / state-emit production path (not just a serializer unit test). Test: drive a turn through the handler and assert the quests projection is emitted to the socket.'
      - 'AC5 — Does NOT regress the legacy quests dict consumers or the wire-parity omission contract for genuinely-empty fields. Test: existing test_wire_parity assertions still hold for empty case.'
    completed: '2026-06-04'
    branch: feat/77-8-quests-spine-client-projection
    review_findings: '3 HIGH: (1) _quests_signature omits objective → objective-only record_quest updates never re-broadcast (AC2 violation); (2) signature joins free-text fields with :/,/| delimiters → narrator content collides distinct spines, suppressing real re-emits; (3) AC4 wiring test only proves import + direct call, never drives the production handler path. Combined fix: derive signature from build_quests_payload(snapshot).model_dump_json() + add a fixture-driven handler-path wiring test. Plus medium/low: setattr-before-emit ordering, AttributeError on dict-valued quest_log, undocumented objective player-facing contract, no empty-skip telemetry. Back to TEA for RED rework.'
    review_verdict: approved
