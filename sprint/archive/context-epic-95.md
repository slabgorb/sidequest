# Epic 95 Context

## Title
Per-Location Orrery — Sector Charts as Region-Scoped Local Maps

## Overview
Sector worlds (perseus_cloud first) get a per-system orrery instead of a single world-wide chart. Content: one sector-tree orbits.yaml (PERSEUS CLOUD hub -> 34 system stars -> planets/belts/stations -> moons), authored by deriving membership from perseus_cloud.sector.json with synthesized Kepler geometry (the export carries none). Engine already renders per-system via scope drill + animates off the beat clock; the missing piece is binding the chart to the party's location node so it acts as a LOCAL map. region_id == star body_id by construction (clean identity join). Content landed in sidequest-content#383. Ref: ADR-094 (orrery), ADR-130 (orbital clock). 'Set a course' travel mechanics are explicitly out of scope.

## Metadata
- **Epic ID:** 95
- **Repo:** sidequest-server,sidequest-content

## Background
_Cross-story constraints and guardrails to be filled in as the epic
progresses._

---
_Generated by `pf context create epic 95` from the sprint YAML._
