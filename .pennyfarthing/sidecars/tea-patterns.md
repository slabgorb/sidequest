## TEA Patterns

### How to work with Keith
- **Senior architect, 30-year dev, learning Rust via the SideQuest port.** Talk at the architectural abstraction level — pattern names (trait objects, actor model, CQRS, composition), trade-offs, system decomposition. Don't over-explain general programming concepts.
- **Teaching mode on by default.** Explain Rust idioms as they come up, framed against the Python equivalent first (the port is Python→Rust), then Go when Python can't express the concept (traits → Go interfaces, ownership, compile-time guarantees). `#[derive(Serialize)]` ≈ `@dataclass`; traits ≈ Go interfaces, not Python ABCs.
- **Dictation artifacts expected.** "axiom" → "axum", "SERG" → "serde", "playlist" → "playtest", etc. Parse for intent before correcting.
- **Procedural world generation is a 30+ year domain for Keith** — MUSHcode, populinator, conlang markov, townomatic, gotown. SideQuest content systems are mature patterns he's rebuilt before, not novel experiments. Never treat conlang/NPC registry/tropes/POI as new.
- **Velocity calibration: ~20x human dev speed, sustained since Nov 2025.** Divide "human dev" estimates by 5-10x for parallelizable work. There is no "too much for one session."

### Core wiring patterns
- **Wiring means non-test consumers.** Before declaring GREEN, grep for the function/struct in production code paths. Library functions with no consumers are stubs, not features.
- **Wiring means the full pipeline.** "Wire X into Y" = source → intermediate layers → rendered output, not "component accepts props." ACs and tests must cover the full data flow.
- **Wiring means visible in the GM panel.** Every subsystem emit site gets an OTEL watcher event with content the lie detector needs to refute a Claude hallucination (e.g. `room_count`, `hp_delta`, `item_added`). Internal data flow without OTEL is not wired.
- **Combat wiring trace (reference):** IntentRouter → Orchestrator → lib.rs → Protocol → UI CombatOverlay. This is the template for verifying any intent-to-UI pipeline.
- **Debug, don't rebuild.** The code is ~98% done — find the small break. Trace the existing pipeline front-to-back before writing anything new. Dev agents repeatedly rebuild pipelines that already work.
- **Source-level wiring guard tests.** When a runtime integration test is impractical (dispatch_connect has 55 params, dockview needs real DOM sizing, bool CLI defaults), read the source file from the test and assert the wire exists as a substring/pattern within N lines of the emit site. Pattern reference: `map_telemetry_wiring_tests.rs` in sidequest-server, `gameboard-wiring.test.tsx` in sidequest-ui. These catch "someone deleted the wire in a future refactor" at `cargo test` / `vitest run` time, not at playtest time.

### Architectural patterns
- **Rust vs Python split.** If it doesn't operate an LLM, it goes in Rust (game engine, state, protocol, markov chains, name gen). If it runs model inference (Flux, Kokoro, ACE-Step — *not* Claude), it's in the Python daemon. Claude CLI calls go through Rust subprocess.
- **Sidecar tool pattern** (Epic 20): Narrator calls tool → parser validates → `assemble_turn` consumes. Established by `item_acquire`, `merchant_transact`, `lore_mark`. All future narrator-exposed tools follow this pattern.
- **Script tool prompt pattern** (ADR-056): Expose Rust generator binaries to the narrator via skill-style checklists (carrot), not threatening gate language (stick). Clear command table, "when to call" guidance, post-call checklist. Never "You MUST" or "Do NOT" — LLMs bash against hard gates.
- **No server-side re-invocation of the LLM to judge the LLM.** Post-hoc validation lives in OTEL spans ("was the tool used? did output match state?"), not a second LLM round-trip. The user calls this the "God lifting rocks" problem.
- **React `key` as layout reset tool.** Changing `key` on a stateful component forces React to unmount and remount it fresh — cleanest way to reset dockview/three.js/etc. without imperative reset code. Used in `GameBoard key={currentGenre}` to fix the Dockview drift bug.

### Content-mechanics stories span two repos (71-24)
- **A genre-pack mechanics story scoped `repos: sidequest-content` still needs a `sidequest-server` test branch.** The YAML bank lives in content, but the TDD tests are Python and exercise the server seams: the loader (`sidequest.genre.loader.load_genre_pack`), `RulesConfig.intent_verbs_by_type`, `confrontation_intent_validator.validate` (the real intent→`matched_type` path), and the production seating+resolution path (`instantiate_encounter_from_trigger` + `dispatch_dice_throw`). Create a matching `feat/<id>-…` branch on sidequest-server, commit the tests there, and log a Delivery Finding so SM-finish handles PRs in BOTH repos.
- **Canonical real-pack confrontation test to mirror:** `sidequest-server/tests/server/test_space_opera_swn_combat_e2e.py`. Helpers `_seated_combat` (seats via `instantiate_encounter_from_trigger` with `NpcMention(side="opponent")`) + `_throw` (`face=[20]` winning d20 through `dispatch_dice_throw`) + the `otel_capture` fixture (`tests/server/conftest.py`). Combat resolves on `enc.outcome == "player_victory"` after opponent `hp.current <= 0`; assert the `encounter.resolved` span `source == "hp_depletion"`. This is the sanctioned exception to "tests don't point at content."
- **A new SWN `combat`+`hp_depletion` confrontation MUST seed `opponent_default_stats` hp/armor_class/dexterity** or the loader's cross-field validator (`rules.py` ~528-565) raises at load — fail-loud by design so the content author finds it before a player triggers the encounter.
- **Orbital/orrery content is the same two-repo pattern, different seam (98-1).** A `repos: content` story that authors a `systems/<id>.yaml` or `orbits.yaml` orrery file tests against the server's orbital seams: `OrbitsConfig.model_validate` (the loader's parse path, loader.py:52 — `extra="forbid"` + `_validate_parent_refs` reject malformed/dangling content loudly), `load_orbital_content` (stage the file as `<tmp>/orbits.yaml` since the loader still hard-codes that name until ADR-141 S1 lands), and `_resolve_scope_center(orbits, Scope.system_root())` (render.py:826 — enforces *exactly one* parent-less body and returns the chart-center root). Canonical real-content orbital test to mirror: `tests/orbital/test_render_coyote_star.py` (uses the `content_dir` conftest fixture, `parents[2].parent / "sidequest-content"`). Pin exact `period_days`/`epoch_phase_deg`/`semi_major_au` against source to catch transcription drift.
- **Intent reroute is verb-set membership, not a code change.** Moving a verb between confrontation types = moving it in the YAML `intent_verbs` list. `validate()` scores by token overlap (`tokenize` strips light suffixes; `-ss` and ≤5-char words like "swing" survive), most-overlap wins, ties by pack-declaration order. To prove a reroute: assert the verb is in `intent_verbs_by_type[newtype]` and ABSENT from the old type, then drive a real action string through `validate()` and assert `matched_type`.

### Git / branch patterns
- **Gitflow on every subrepo.** Subrepos (sidequest-server, sidequest-ui, sidequest-content, sidequest-daemon) target `develop`. Only the orchestrator targets `main`. Always check `repos.yaml` before any git op.
- **Branch before editing.** `git checkout -b feat/description` BEFORE touching files, not after. Avoids the "hook-blocked commit → panic → stash scramble" failure mode.
- **Dirty work comparison pattern.** When local uncommitted changes conflict with a pull: commit dirty work to a temp branch, pull clean on the default branch, diff each file local-vs-remote side by side, categorize (identical / remote better / local has extra value / no remote change), then cherry-pick deliberately. Never stash.
- **Additive git ops need no permission** (commit, push non-force, pull --ff-only, checkout existing, checkout -b, fetch, add). **Destructive git ops always ask first** (reset, clean, force-push, branch -D, rebase -i, stash — banned outright).

### Playtest patterns
- **Pingpong cadence: every 2-3 minutes during active playtest.** Read `/Users/keithavery/Projects/sq-playtest-pingpong.md` frequently so new `open` items at the top don't pile up unnoticed. Update to `fixed` immediately when a fix lands — don't batch.
- **Always pull and test on new commits.** When `git log HEAD..origin` shows new commits during playtest: pull → rebuild → restart → test. No "want me to?" prompt.
- **Always rebuild on restart.** Every service restart during playtest is a full rebuild. Compile time is negligible vs. debugging a stale binary.
- **Keep going.** Work through the checklist autonomously during playtest. Don't pause to ask "want me to continue" — Keith wants momentum.
- **Restart in existing tmux panes.** Send Ctrl+C and re-run the command. Don't close/reopen panes — churn annoys Keith and wastes time.
- **Source-grep tests > integration tests for playtest fixes.** Faster to write, faster to run, catches the exact failure class (someone unwired it in a refactor) without needing a full stack harness.

### Spec hierarchy
- **Spec authority order:** Story scope (session file, highest) → story context → epic context → architecture docs / SOUL / rules (lowest). When sources conflict, the session scope wins. Log deviations **at the moment of the decision**, not at phase exit.

### Verify the session's "current state" against actual green tests before writing RED (2026-06-01, story 65-8)
- **What happened:** 65-8's session file described its core problems as "POI images never appear" and "POI slug manifest unused" — and proposed a 7-AC build. Reconnaissance (Explore agent) + running the *existing* `test_reference_poi_images.py` showed story **63-8** had already shipped that wiring (12 tests green). The session file was describing a *pre-63-8 world*. Only ~1 of the 7 ACs (the R2-manifest existence gate) was genuinely net-new; AC2 was done, AC5's type-chip was already in the live card markup, AC4 was ambiguous.
- **Why it matters:** Writing RED tests for the full session frame would have produced several green-on-arrival tests (duplicating 63-8) — not true RED, and vacuous per python-review check #6. A story's "Background/Problem" prose is a *snapshot at authoring time*; sibling stories can land between setup and RED and silently invalidate it.
- **How to apply:** Before writing RED, (1) dispatch an Explore pass to map the REAL symbols/signatures (don't trust session-file names — though here they were exact); (2) **run the existing test files for the subsystem** and read which behaviors are already green; (3) diff session ACs against green reality; (4) if material overlap, surface it to the boss with a crisp table + recommendation (re-scope/full/escalate) via AskUserQuestion rather than silently narrowing or silently bloating; (5) log the chosen scope as a `major` Design Deviation so Reviewer doesn't expect the dropped ACs' code in the PR. The net-new RED here: a loud-fail manifest loader, an existence gate at the real route (gated fixture world with one in-manifest + one authored-but-absent POI), and a `manifest_loaded` OTEL span — all failing for honest reasons, with the gate test literally capturing the broken `<img src=.../sunken-vault.png>` the old authorship-gate emits.
- **Setup-gap note:** sm-setup wrote the session file but NOT `sprint/context/context-story-65-8.md`; the TEA context gate (`pf validate context-story`) hard-blocks RED on exit 2. Fix is `/pf-context create story <id>` (or re-run SM), not auto-authoring it from the TEA seat.

### Make a fail-loud test a TRUE RED via the typed loader, not the generic walk (2026-06-03, story 65-12)
- **What happened:** 65-12 (lore-page world Timeline section) needed an AC1 "malformed legends.yaml → HTTP 500, never a silent timeline-less 200" test. But the lore page ALREADY walks `legends.yaml` through the generic humanization walk (`_file_renders_by_stem`), which renders raw YAML and does NOT validate via the `Legend` model. A malformed-*YAML* fixture (unterminated, like 65-11's map_malformed) might already 500 today via the existing walk → the test would be green-on-arrival, not RED.
- **Why it matters:** A fail-loud contract test only proves anything if it fails for the *new* code path. `Legend` is `extra="forbid"`, so a fixture legend with a **valid-YAML-but-schema-forbidden key** (`bogus_unknown_field: ...`) renders fine through the generic walk (200 today) and only raises when the NEW typed loader (`_load_legends_flexible` → `Legend.model_validate`) runs. That makes the 500 assertion a clean RED that specifically pins the new loader's No-Silent-Fallbacks behaviour (and forbids Dev wrapping the load in a swallowing try/except — python rule #1).
- **How to apply:** When a subsystem already touches a file generically, don't reach for malformed *syntax* to force the loud-fail RED — reach for a payload that only the **new typed/validated path** rejects (extra-key on an `extra="forbid"` model, a missing required field, a wrong-type scalar). Verify via testing-runner that the happy-path fixtures return 200 (no fixture-load 500s) so every other RED failure is a clean contract assertion, not a fixture bug.
- **Section-scoped HTML helper:** reuse the 65-11 map-test shape — a `_section(html)` helper that slices `<section id="X">…</section>` and fails loud if absent. Every assertion scopes to that slice so the *generic* legend/POI rendering elsewhere on the page can't satisfy a section assertion by accident. Route-driven only; no source-text wiring tests (server CLAUDE.md).

### Wiring a "dead" data-only subsystem (epic 82 shape): consumer + OTEL + player field, all one RED file (2026-06-03, story 82-8)
- **What happened:** 82-8 (ADR-021 track 3) wired `WealthTier` gold→label, which had *zero* repo-wide consumers (only the pydantic model + `__init__` export — `grep -rn WealthTier` proves the audit's "data-only" call exactly). The either/or spec ("wire `narrative_weight` OR `WealthTier`") forced a seam choice at RED, since TDD needs concrete failing tests. Chose WealthTier: pure-function core (ideal for the boundary AC), and an existing player surface to extend (`InventoryPayload.gold`/`currency_name` built in `views.party_member_from_character`).
- **Why it matters:** Epic 82 ("Surface the Dark Subsystems") stories all share the shape *find dead seam → add production consumer → emit OTEL → wiring test*. The "done" bar is higher than "code runs": a real consumer AND an OTEL emit AND a player-facing surface, or the wiring doctrine says it isn't live. A pure-unit test alone fails that bar.
- **How to apply:** Decompose the RED into 4 contract layers, each its own test class, **local-importing the not-yet-existing symbol inside each test** so a missing symbol fails *only that AC* (not collection): (1) pure resolver with parametrized boundary/one-above/over-cap/zero/negative + empty→None; (2) the new player-facing protocol field (`InventoryPayload` is `extra="forbid"` → unknown kwarg raises a clean ValidationError RED today, defaults-None pins the no-data case); (3) OTEL span by name via the `otel_capture` fixture (`tests/server/conftest.py:1177`) — `[s for s in otel_capture.get_finished_spans() if s.name == "..."]` with a failure msg listing seen names; (4) behavioral wiring driving the **real** consumer.
- **Views-layer player-facing wiring harness (reusable):** to drive `views.party_member_from_character` for real, copy the `_char(name)` (Character + `CreatureCore`/`Inventory`/`HpPool`) and `_sd(...)` (`_SessionData` with `load_genre_pack(...)`, `MagicMock()` for repository/dungeon_repository/telemetry_sink/orchestrator) builders from `tests/server/test_multiplayer_party_status.py`, then `handler = WebSocketSessionHandler(save_dir=Path("/tmp/sq-test-saves"))` and `sd.snapshot.character_locations = {}` to skip the world-location path. Genre models are mutable + not frozen (`ProgressionConfig` is `extra="forbid"` but assignable) so you can **inject** known data onto the loaded pack (`pack.progression.wealth_tiers = [...]`) — the test owns the boundary data instead of coupling to whatever a real pack authors today.
- **No-Silent-Fallbacks pairing:** always pair the happy-path wiring test with a *negative-invariant* test — no authored data → label `None` AND span does NOT fire. The negative span test is green-on-arrival in RED (no consumer yet), but it's a real invariant that must keep holding through GREEN (a fired span on empty data would be a GM-panel lie). Log the seam choice + any unspecified edge semantics (negative→floor, empty→None) as `minor` Design Deviations so Reviewer knows TEA narrowed the either/or.

### Cross-repo contract test: load the sibling repo's models for real, fail-loud not skip (2026-06-10, story 101-1)
- **What happened:** 101-1 (epic 101 split-brain remediation) needed a test holding the deliberately-duplicated daemon↔server renderer-model seam (`RenderTier` enum + `StageCue` fields/`camera` type). The drift: daemon still had `CARTOGRAPHY` (gone server-side 2026-04-28) + two zimage configs, and `StageCue.camera` was `CameraPreset` enum vs the server's plain `str`. A *snapshot* contract (hardcoding the expected server shape in the daemon test) would only hold the daemon side against a frozen expectation — it would NOT catch the server changing. The real seam needs both live sides compared.
- **Why it matters:** Daemon and server are sibling subrepos under the orchestrator checkout (`oq-N/sidequest-{daemon,server}`). The server's `renderer/models.py` depends only on pydantic + stdlib, so it loads in isolation in the daemon test env **without importing the whole server package** (no server deps needed). That makes a genuine cross-repo comparison cheap. Story 78-3's TEA log warned "a daemon-repo test reaching another repo is fragile and wrong-repo" — but that was a *documentation* obligation (ADR notes) verifiable by review; a wire-contract test inherently MUST compare both sides. Don't let the 78-3 precedent talk you out of a legitimate contract test.
- **How to apply:** Loader pattern — `importlib.util.spec_from_file_location("…_contract", models_path)` + `module_from_spec` + `spec.loader.exec_module(module)`, where `models_path = Path(__file__).resolve().parents[N] / "sidequest-server" / …` with an `os.environ["SIDEQUEST_SERVER_ROOT"]` override (precedent: `telemetry/watcher_bridge.py` uses `SIDEQUEST_SERVER_URL`). Then compare `{t.value for t in DaemonTier} == {t.value for t in module.ServerTier}`, `set(DaemonModel.model_fields) == set(module.ServerModel.model_fields)`, and `DaemonModel.model_fields["f"].annotation == module.ServerModel.model_fields["f"].annotation` (pydantic v2 resolves annotations even under `from __future__ import annotations`). **Fail loud, never skip:** raise `FileNotFoundError`/`ImportError` when the server file is absent — a `pytest.skip` is a silent fallback that stops holding the seam and gives false confidence (the exact defect the epic remediates). In the orchestrator checkout the server is always present, so fail-loud never false-positives. Log the sibling-reach + the no-skip choice as `minor` deviations.
- **Coherent-RED for a deletion refactor:** removing an enum member ripples wider than the model file. `CARTOGRAPHY` lived in 5 production sites — the enum, two `ZIMAGE_*_TIER_CONFIGS` dicts (these reference `RenderTier.CARTOGRAPHY` directly, so a partial removal is an *import-time* AttributeError → the removal is self-forcing), plus string-keyed leftovers in `daemon.IMAGE_TIERS` and the worker `_TIER_TO_R2_KIND` (raw strings, won't crash → silently dead). Add **invariant guard tests** (`IMAGE_TIERS ⊆ {t.value for t in RenderTier}`) that are green now and only flip RED on a *partial* removal — they force Dev to delete the dead routing strings too. And **update, don't leave, the pre-existing tests that encode the old contract** (enum-identity `camera`, `cartography` as a sample render tier) — TEA owns all test changes so Dev touches only source; otherwise the suite can never reach GREEN (contradictory assertions). Find them with `grep -rn` for the symbol/string across BOTH production and tests before writing RED.

### Dead-surface removal touching extra=forbid models + persisted saves needs a migrations.py shim (2026-06-10, story 101-2)
- **What happened:** 101-2 (epic 101) deletes the dead voice-generation surface from the server. Four of the five surfaces are pure deletion (genre voice models, `AudioConfig.creature_voice_presets`, `GenrePack.voice_presets`, `MessageType.VOICE_SIGNAL/VOICE_TEXT` + the `VOICE_TEXT` `TARGETED_KINDS` route). The fifth — `Npc.voice_id` — is the load-bearing one: `Npc` is `model_config={"extra":"forbid"}`, so a pre-removal Postgres save carrying `voice_id` would raise `ValidationError` on load the instant the field is deleted. RED must drive a backward-compat shim, not just an absence assertion.
- **Why it matters:** The shim has a *canonical home* and a copy-paste pattern. `sidequest/game/migrations.py`'s docstring is explicit: "this module is the ONLY place backward-compat shims live" — it runs read-old-write-new on the snapshot dict BEFORE pydantic validation, via `migrate_legacy_snapshot`'s sub-function tuple (`_migrate_s1..s5`). Each sub-function returns OTEL attrs or `None`, and the orchestrator emits one `snapshot.canonicalize` span. The matching extractor allow-list lives in `sidequest/telemetry/spans/persistence.py::_extract_snapshot_canonicalize` ("no defaulted keys"). The exact precedent to clone is S3 (`tests/game/test_party_location_migration.py`): a `_migrate_s{N}_…` strip/seed fn + a `s{N}_…` counter key in the extractor.
- **How to apply:** Drive GREEN toward `_migrate_s6_strip_npc_voice_id` registered in the orchestrator tuple + `s6_voice_id_stripped` added to the extractor allow-list. RED set = (1) `hasattr(migrations, "_migrate_s6_…")` wire test; (2) build a legacy snapshot with `npcs[].voice_id` and assert `"voice_id" not in migrated_npc` — this is the genuine RED driver; (3) OTEL: route-extractor `SimpleNamespace` test + in-memory `InMemorySpanExporter` span-fires test (clone S3's two OTEL tests verbatim). **Gotcha — the "validates under extra=forbid" test is a GREEN-side guard, NOT a RED driver:** today `voice_id` is still a valid `Npc` field, so `Npc.model_validate(dict_with_voice_id)` passes *now* and *after* removal — it never goes RED. Keep it as a regression guard but lean on the `"voice_id" not in migrated` assertion for the actual RED.
- **Scope guards earn their keep on removal stories:** grep the symbol broadly first — `voice` matched `MixerConfig.voice_volume` (audio mixer dial) and `ChassisVoiceSpec` / `register_chassis_voice_section` (ADR-125 chassis voice), neither of which is TTS. Write *retention* tests (`assert "voice_volume" in MixerConfig.model_fields`, `assert hasattr(gm, "ChassisVoiceSpec")`) so an over-eager deletion flips them RED. AC-3 "grep-guard" → reflection (`hasattr`/`model_fields`/enum+dict membership), never `read_text()` (server CLAUDE.md "No Source-Text Wiring Tests"). And own the stale presence-tests: deleted the two `VOICE_*` wire-string tests in `test_enums.py` and flipped `test_message_type_complete_count` 56→54 (TEA touches all tests so Dev touches only source).

### Scene-harness hydrator: extend a block by cloning the 50-2x section idiom + a real-engine wiring proof (2026-06-10, story 90-4)
- **What happened:** 90-4 adds two new fixture blocks to `sidequest/game/scene_harness.py`: character `effort:`/`spellcasting:` → `core.effort`/`core.spellcasting`, and encounter `win_condition:`/`category:`/`actors:` → `StructuredEncounter`. All hydrator tests live in ONE file (`tests/game/test_scene_harness_hydrator.py`, ~2700 lines) sectioned per story (50-18…50-23). Append a new `# ── Story 90-4 ──` banner + a `_write_*_fixture` tmp_path helper; don't start a new file. The hydrator's established contract does all the heavy lifting: pydantic `model_validate` on the nested model + re-wrap `ValidationError` as `FixtureValidationError` (HTTP 422), `is not None` guard so present-but-malformed fails loud, model defaults (`None`/`{}`/Literal default) preserved when the block is omitted.
- **Why it matters:** The load-bearing RED isn't "the field hydrates" — it's "the hydrated seed actually drives the engine." A hydrator can construct a valid-looking `SpellcastingState` whose economy the cast spine rejects, and unit tests would pass while the deterministic proof fails. Two wiring tests close that: feed `core.spellcasting` through the REAL `WwnRulesetModule.resolve_spellcast` (assert `cast=True` + one cast spent), and drive `check_hp_depletion` over hydrated actors/cores (assert `player_victory` with opponent at 0 HP). These are the actual ACs when the story is "prove X live." Mirror the call shapes from `tests/game/ruleset/test_wwn_spellcast.py` (`WwnConfig(attribute_map=...)`, `random.Random(0)`, no-save/no-damage `CastInput` to stay target-free) and `tests/game/test_hp_depletion.py` (`check_hp_depletion(enc, lambda n: cores.get(n))`).
- **How to apply:** RED set per new block = (1) happy-path hydrate asserting exact values; (2) omit → model default (backward-compat, passes NOW — keep as regression guard); (3) loud-fail trio: not-a-mapping/list, `extra=forbid` typo, bad-value-type → `FixtureValidationError` (these fail with "DID NOT RAISE" today because the hydrator silently drops unknown blocks — that IS the RED driver); (4) one real-engine wiring proof; (5) canonical-fixtures-unaffected lock (`combat_brawl_wasteland` et al. stay `spellcasting=None`/`effort={}`/`dial_threshold`). **Deterministic-fixture gotcha:** a content-only proof must seed `prepared` NON-EMPTY — chargen (`seed_wwn_magic`) seeds `prepared==[]` (chosen at rest), so an empty list makes every cast refuse `'<spell>' is not prepared`. Log that divergence as a deviation; Dev must hydrate `prepared` verbatim with no chargen-style truncation. **Effort block shape:** keyed by source (`effort: {high_mage: {max: 3}}`), hydrator derives `EffortPool.source` from the key — matches `core.effort.get(source)` read in `wwn.py`. Flag the keyed-vs-explicit-source choice for Architect SPEC-CHECK.
